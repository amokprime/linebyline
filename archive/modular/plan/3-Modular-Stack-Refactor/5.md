---
model: GLM-5.2
provider: https://chat.z.ai
start: "Sep 14, 2026"
---
## Turn 1

### User:

Read `AGENTS.md`, `README.md`, `MEMORY.md`, and the `*-SKILL.md` files. Follow skill files and `AGENTS.md` as rules. This is the Onboard step.

**Attachments:**
`1789428416595049443.zip`

---
### Agent:

Now let me check the directory structure more carefully to derive the current version, and briefly look at the roadmap for current state context.
Onboard complete. Here's what I've absorbed from the bundle:

#### Onboard summary

**Project:** LineByLine — an opinionated single-file web app for manual line-by-line LRC lyrics syncing, currently mid-modular-refactor (Phase D in progress, monolith at `docs/index.html`, scaffold at `src/`).

**Current app version:** **0.37.2** (derived from `archive/semantic/0.37.2/` references throughout MEMORY.md and the roadmap — highest semver folder; `archive/modular/` has only `plan/`, no version dirs yet).

**Phase state (from MEMORY.md):**
- Phase A–C complete (scaffold, theme tokens, six Vue tranches, 103 specs)
- Phase D Tranches 1–4 done (useAppState/usePersistedState/useUndoRedo, useAutosave/useTitle, useModeSwitch, useAudio) — 231/231 Vitest specs green, `vue-tsc -b` clean
- PR #11 open with modular refactor work; 50 → 29 OPEN Sonar issues (remaining are Accept/FP dispositions)
- Phase E cutover (Playwright target flips from `docs/index.html` to Vite build output) is when the modular stack becomes the live app

**Key invariants I'll honor every turn:**
1. **Web-channel rules** — deliverables go in `/home/z/my-project/download/` only; flat hyphenated filenames (no subdirs visible to user, no spaces); chat-only output for non-code; re-read web-channel skill if context compacts.
2. **Pre-patch checklist** — request clarification when unclear; patch with minimal diff; consult code-quality skill before modifying functions.
3. **Post-patch verification** — syntax check, section-index re-grep if line numbers shifted, spot-check referenced functions, trace one user action, run `npm run test:unit` if `src/` was touched.
4. **Post-turn updates** — keep MEMORY.md / skills / section-index current every turn (user may push at any moment). One bullet per parallel thread, not per turn. Apply inline in `download/`, not as drafts.
5. **Deliver zip pattern** for multi-file sessions — `scripts/prepare.sh` zips `download/` into `deliver.zip` (with `deploy.sh` mapping flat names to repo paths); user's `dpl` runs `unpack.sh`.
6. **No coding at Onboard** — per project-workflow step 4, the app HTML and domain skills arrive in a later Repomix bundle (Build / Review / Test / Skills / Propose). Onboard may be the only step if this is a planning session.
7. **Version derivation** — user states version once; I apply it consistently to `<title>` and any version comments. Don't substitute dots in filenames.
8. **SonarCloud / CodeQL** — the `sie` Markdown report the user uploads is canonical; sandbox API is a fallback only. Closed issues have `line: null` — check `status`/`resolution` before triaging.
9. **`MAX_LINES=500`, `MAX_SECONDARIES=10`**, `S3776` CC threshold = 15, undo/redo single-push model (pre + post only on wholesale replacement).
10. **No external fonts.** No Python port. Web is the only forward path.

**Per Onboard step 5:** no companion file created.

Ready for the next step — name the step (Build / Review / Test / Skills / Propose) and upload the matching Repomix bundle, or tell me what you'd like to work on.

---
## Turn 2

#### User:

Re-read `project-workflow-SKILL.md`. Follow `*-SKILL.md` files and `AGENTS.md` as rules. This is the Build step. Continue [[archive/modular/plan/0-Roadmap#3. Modular Stack Refactor (Vite + Vue + Tailwind + shadcn-vue)|0-Roadmap]] at Phase D Tranche 5. Then cleanup unfenced code in `0-Roadmap.md` per `AGENTS.md` such as:
```
[ti:]/[ar:]
 #main-scroll / #main-textarea / #main-lines
[0.1, 4]
```

**Attachments:**
`1789428571602600416.zip`

---
### Agent:

I have the Build Repomix. Let me set up the workspace and extract the files I need.
Per project-workflow-SKILL.md, I'm re-reading project-workflow-SKILL.md (I have it from Onboard), and now reading the Build-relevant domain skills, MEMORY.md (already have from onboard — I'll re-read since the Build step explicitly says so), and the roadmap.
Now let me re-read MEMORY.md (I have it from Onboard but Build step requires it). Now let me find the Roadmap — it's in the onboard pack.
I have the Tranche 5 spec. Now I need to read the relevant monolith sections in `docs/index.html` to understand exactly what to port. Let me grep for the section markers:
Now let me read the key monolith sections for Tranche 5: Render/UI, Sync/timestamp:
Now let me look at the existing src/ files I need to work with. Let me read the current state of useAppState, useAudio, useModeSwitch, EditorArea, and App.vue.
Now let me look at the existing pasteHandlers.ts (for cleanPaste, normalizeLrcTimestamps already moved, etc.) and geniusExtractor.ts:
Now let me look at the existing test files to understand the test patterns I need to follow, especially for composables:
Now let me look at one of the existing utility tests to understand the pattern for the four new functions I need to port:
Now let me check the existing logic.spec.js Playwright file to understand the four functions to port:
Now I have a complete picture. Let me check the existing package.json to confirm test commands, and look at the existing geniusExtractor test pattern:
Now I have enough context. Let me update the todo list and start implementing.
#### Plan

**Tranche 5 — Sync/timestamp scope:**

1. **Pure helpers → `src/utils/timestampSync.ts`** (new file):
   - `peelLastParen` (from `_peelLastParen`)
   - `findNextTimestampMs` (from `_findNextTimestampMs`)
   - `findPrevTsMs`, `findNextTsMs`, `findRunEnd` (internal helpers for `assignInterpolatedTs`)
   - `assignInterpolatedTs` (from `_assignInterpolatedTs`)
   - `batchSplitParens` (uses `peelLastParen` + `assignInterpolatedTs`)
   - `findNextUnprocessedSplit`, `findNextNonMetaFromIdx` (for `_advanceAfterSplit`)

2. **`src/utils/lrcParser.ts`** — add `hasLyricContent(text)` and `hasTrailingTimestamp(lines)` (pure, take text as param instead of reading DOM).

3. **`src/composables/useSync.ts`** (new file) — stateful:
   - `syncLine`, `insertEndLine`, `adjustTs`, `markAsTranslation`
   - `seekPrevLine`, `seekNextLine`, `replayActiveLine`
   - `doSyncFile`, `tickSeekOffset`, `setOffsetMode`
   - `updateActiveLineFromTime`, `renderMainLines`, `scrollToActive`, `scrollToPlaying`
   - `_handleLineClick`, `_handleLineClickPlain`, `_insertSyncTrailing`, `_advanceAfterSplit`
   - `onMainPaste`, `onMainInput` (textarea handlers)

4. **Wire**:
   - `EditorArea.vue` — implement `renderMainLines`, add `@input`/`@paste` on textarea, `@paste` on `#main-lines`, attach click handler on lines, two-way bind textarea
   - `App.vue` — `initSync(refs, callbacks)`, wire real `renderMainLines` + `updateActiveLineFromTime` + `scrollToPlaying` + `announce` to useAutosave/useAudio/useModeSwitch
   - `LeftPanel.vue` — seek-offset input `@change` + arrows `@click` + sync-file-btn `@click`

5. **Tests** — Vitest for `timestampSync.ts` (pure helpers), `useSync.ts` (stateful), and additions to `lrcParser.test.ts`.

6. **Post-patch**: `vue-tsc -b`, `npm run test:unit`, trace one user action (sync a line).

7. **Docs**: update MEMORY.md (Phase D Tranche 5 bullet), update `0-Roadmap.md` (mark Tranche 5 done) + cleanup unfenced code per AGENTS.md.

Let me set up the project and start writing code.
I don't have the actual repo's package.json. Per the Build Repomix pattern, the package.json isn't included. Let me extract it from the onboard repomix (which DID include it):
Baseline: 231/231 specs green. Now let me start implementing.

#### Step 1: Add hasLyricContent + hasTrailingTimestamp to lrcParser.ts
#### Step 2: Create src/utils/timestampSync.ts
#### Step 3: Create src/composables/useSync.ts

This is the big one. Let me first check what existing imports I need from the LeftPanel wiring — the seek-offset input ref and the seek-arr-back/seek-arr-fwd + sync-file-btn click handlers.
Now I'll create the `useSync` composable. This is the largest piece.
#### Step 4: Wire EditorArea.vue
The `seekOffset` ref is owned by LeftPanel, not EditorArea. Let me refactor — make it a separate setter so each component registers what it owns:
Also update the `useSync` export to include `setSeekOffsetRef`:
Now update EditorArea.vue to remove the placeholder seekOffset ref:
Now update EditorArea's textarea to add `@input` and `@paste` handlers, and add a blank-line CSS rule:
Also add the `.blank-line` CSS rule (for cursor:default on blank lines, matching the monolith):
#### Step 5: Wire LeftPanel.vue — register seekOffset ref, wire sync-file + arrows + change handler
Let me fix the math to be cleaner, and check the template for `onSyncFile`:
The template still references `onSyncFile` which I removed. Let me fix that and simplify the math:
Now update the template to pass `1`/`-1` instead of `1000`/`-1000`, and use `doSyncFile` directly:
#### Step 6: Wire App.vue — initSync callbacks + wire useAutosave + useAudio + useModeSwitch for Tranche 5
Let me verify by running vue-tsc:
Let me fix these TS errors:
Types are clean. Let me run the existing tests to make sure nothing regressed:
All 231 baseline specs still pass. Now let me write the new test files.

#### Step 7: Vitest tests for timestampSync.ts
Let me check the failures and fix the test expectations:

- Test 1: `assignInterpolatedTs` clamps to zero — got `[00:00.04]` (40ms). That's because `nextMs - (count-k)*10` = `50 - 10 = 40`, NOT clamped. The clamp is `Math.max(0, ...)` which means clamping only applies to the WHOLE result. Let me re-read the source.

Looking at the source: `Math.max(0, nextMs - (count - k) * 10)`. So `nextMs = 50, count = 1, k = 0` → `Math.max(0, 50 - 10) = 40`. Not zero. My test assumption was wrong.

- Test 2: `batchSplitParens` with single line — no interpolation because there's no next anchor. The translation gets no timestamp. Let me fix the test expectation.
Now fix the `batchSplitParens` test:
Let me fix the remaining 2 tests:
All 37 timestampSync tests pass. Now let me also add some tests for the new `lrcParser.ts` additions:
#### 第 8 步：useSync.ts 的 Vitest 测试
The test is referring to `mod.ta` but I returned `ta` separately. Let me fix:
All 27 useSync tests pass. Let me run the full suite to verify nothing regressed:
**307/307 specs green** (231 baseline + 12 new lrcParser + 37 timestampSync + 27 useSync). Let me also run vue-tsc to verify types are still clean:
307/307 specs green + `vue-tsc -b` clean. 

#### Step 9: Post-patch verification — trace one user action

Per the post-patch checklist: "if the patch changed any logic, trace one user action through the changed code path to confirm it still reaches the expected outcome."

Let me trace the **sync a line** action (the central Tranche 5 dispatch):
#### Step 10: Update MEMORY.md (Phase D Tranche 5 bullet)
Now I'll add the Tranche 5 bullet after Tranche 4. Let me create a working MEMORY.md copy:
#### Step 11: Update 0-Roadmap.md — mark Tranche 5 done + cleanup unfenced code per AGENTS.md

First, let me find unfenced code in 0-Roadmap.md that needs cleanup per AGENTS.md:
Let me look at the specific unfenced patterns the user mentioned and fix them in `0-Roadmap.md`:
Now I have a clear list of unfenced code patterns to clean up. Let me apply fixes:
Now let me also mark Tranche 5 done and add an implementation notes section. Also need to clean up the remaining unfenced patterns I missed:
All these remaining matches are properly fenced in inline backticks (they have backticks around them). My initial grep was wrong — these are already fenced. Let me verify the unfenced ones I missed by checking what's NOT in backticks:
Only one remaining: line 388 — but it's actually inside a code block (the regex pattern is inside backticks already, just looks like `[ti:` is "inside" the backtick span). That's fine.

Now let me mark Tranche 5 done and add the implementation notes section:
Now let me add the Tranche 5 implementation notes section after the Tranche 4 implementation notes:
Now let me also update MEMORY.md's Tranche 5 entry — I had written `[ti:]` unfenced in some places. Let me check:
Now let me check the MEMORY.md Tranche 5 entry I wrote — I should verify it doesn't have unfenced patterns I introduced:
All clean. Let me do one final verification — run vue-tsc + full test suite:
`vue-tsc -b` clean + 307/307 specs green.

#### Step 12: Copy deliverables to download/ + run prepare.sh
Now copy the deliverables to `download/` with hyphenated names (per project-workflow-SKILL.md). The files I created or modified this session:

**New files:**
- `src/utils/timestampSync.ts`
- `src/composables/useSync.ts`
- `tests/unit/timestampSync.test.ts`
- `tests/unit/sync.test.ts`

**Modified files:**
- `src/utils/lrcParser.ts` (added 3 functions)
- `src/components/EditorArea.vue` (initSync + handlers + CSS)
- `src/components/LeftPanel.vue` (setSeekOffsetRef + button wiring)
- `src/App.vue` (setMainText chain + callback wiring)
- `tests/unit/lrcParser.test.ts` (12 new specs)

**Docs:**
- `MEMORY.md` (Tranche 5 bullet)
- `0-Roadmap.md` (Tranche 5 marked done + impl notes + unfenced code cleanup)

I also need to copy the `deploy.sh` template + `prepare.sh` from the onboard pack (they're committed at `ai/chat.z.ai/scripts/delivery/`). Let me set those up:
Now copy deliverables with flat hyphenated names:
Now I need to write the per-session `deploy.sh` that maps each flat filename to its real repo path. The committed template is at `ai/chat.z.ai/scripts/delivery/deploy.sh`; I need to fill in the file mappings:
Now run `prepare.sh` to zip everything into `deliver.zip`:
`deliver.zip` is built with all 12 files (11 deliverables + `deploy.sh`) and the loose files cleaned up. Let me verify the final state:
Download/ is clean (only `deliver.zip`) and contains all 12 files.
#### Build step complete

**Phase D Tranche 5 — Sync/timestamp** shipped. 76 new Vitest specs (37 `timestampSync` + 12 `lrcParser` additions + 27 `useSync`); full suite **307/307 green** (was 231); `vue-tsc -b` clean.

**What landed:**
- `src/utils/timestampSync.ts` (new) — the four unique-to-`tests/logic.spec.js` functions (`peelLastParen`, `findNextTimestampMs`, `assignInterpolatedTs`, `batchSplitParens`) plus their internal helpers. Phase E consolidation begins here.
- `src/utils/lrcParser.ts` — added `hasLyricContent`, `hasTrailingTimestamp`, `allLyricLinesHaveTs` (refactored to take text/lines as params instead of reading `getTA()`).
- `src/composables/useSync.ts` (new) — ports the monolith Sync/timestamp + Render/UI sections. Owns `renderMainLines`, `syncLine`, `insertEndLine`, `adjustTs`, `markAsTranslation`, `seekPrev/NextLine`, `replayActiveLine`, `updateActiveLineFromTime`, `doSyncFile`, `tickSeekOffset`, `setOffsetMode`, plus the textarea `@input`/`@paste` handlers and `#main-lines` delegated click/paste handlers.
- `EditorArea.vue` — `initSync` + delegated `mousedown`/`contextmenu`/`paste` on `#main-lines`; textarea `@input`/`@paste`; new `.blank-line` CSS class.
- `LeftPanel.vue` — `setSeekOffsetRef` + `onSeekOffsetTick(sign)` wrapper (reads `cfg.seek_offset_tick` at click time) + `doSyncFile` direct binding + `#seek-offset` `@change`.
- `App.vue` — defines `setMainText(t)` (the full side-effect chain: pre-pushSnapshot → write → render → title → autosave → post-pushSnapshot); wires `useSync`/`useAudio`/`useAutosave`/`useModeSwitch` callbacks; `setOnInputCallback` decouples `useSync` from `useUndoRedo` (avoids circular import).
- `MEMORY.md` — Phase D Tranche 5 bullet with port deltas + quirks pinned by tests.
- `0-Roadmap.md` — Tranche 5 marked done + implementation notes section + unfenced code cleanup per `AGENTS.md` (`[ti:]`/`[ar:]`/`#main-scroll`/`[0.1, 4]` etc now properly fenced in inline backticks).

**Delivered as `download/deliver.zip`** — 12 files (11 deliverables + the per-session `deploy.sh` mapping flat names to repo paths). Download and run `dpl` to deploy.

**Next step:** you can stay in Build and tackle Tranche 6 (Merge + Secondary fields — `useMerge` + `SecondaryField.vue` wiring), or jump to Review to audit the Tranche 5 patches against the code-quality / sonarqube / aria skills before pushing. Tranche 9 (the global keyboard handler) is the natural capstone — it dispatches to the ~30 actions `useSync` now exposes.

---
## Turn 3

#### User:

Stay in Build for tranches 6 and 7. Then do a pre-push review pass with the review Repomix before deployment, and I'll let you know if any issues survive the review.

**Attachments:**
`1789431616513564438.zip`

---
### Agent:

The new zip is the **Review Repomix** — I'll set it aside for the post-build review pass. First, let me verify my workspace is intact and continue with Tranches 6 and 7.
Workspace intact: 307/307 specs green. Now let me re-read project-workflow-SKILL.md's relevant sections for Build + read the relevant monolith sections + current src files for Tranches 6 and 7.
#### 第 6 阶段 — 合并 + 次要字段

让我先读取相关 monolith 部分（次要字段 + 行数/合并）以及当前的 SecondaryField.vue + MenuBar.vue：
Now let me read the current SecondaryField.vue, MenuBar.vue, and the secondaryField test:
Now I have a complete picture. Let me look at the `controlsPanel.test.ts` to understand patterns:
Now I have everything I need. Let me design Tranche 6 + 7.

#### Tranche 6 — Plan

**Pure helpers to extract (`src/utils/lrcParser.ts` additions or new `src/utils/secondary.ts`):**
- `getMainLyricLines(lines)` — filters non-meta non-blank non-trailing-ts lines
- `getSecLines(text)` — filters non-blank lines from secondary textarea content
- `updateFieldBorders(editorAreaEl)` — DOM helper for visual border on last visible column

**`src/composables/useMerge.ts` (new):**
- `addSecondary()` — push to secondaryPool; reuses hidden pool entry if available
- `removeSecondary()` — hide last visible column (pool entry stays for reuse)
- `checkLineCounts()` — update warn bars + merge button enable state
- `updateMergeBtn()` — enable/disable merge button based on preconditions
- `mergeTranslations()` — build merged LRC with interpolated timestamps
- `syncScrollFrom(src)` — scroll sync across main + secondary fields
- `syncSecScroll()` — wrapper that passes main-scroll as src
- `onSecInput(idx, e)` — textarea @input handler (collapses 3+ blanks, updates pool text, schedules snapshot)
- `onSecPaste(idx, e, parenChecked)` — textarea @paste handler (Genius cleaning, paren wrap)
- `onSecScroll(e)` — textarea @scroll handler
- `onSecFileImport(idx, file, parenChecked)` — per-field file picker handler

**Wire:**
- `SecondaryField.vue` — props `{ index }`; binds textarea `:value` to pool entry text; `@input`/`@paste`/`@scroll`/`@keydown` handlers; paren checkbox reactive; file picker wired
- `MenuBar.vue` — Add/Hide/Merge buttons reactive (disabled from `secondaryPool.length` + `mergeDone` + merge preconditions)
- `App.vue` — `setSyncCallbacks({ checkLineCounts, updateMergeBtn, syncSecScroll })`
- `EditorArea.vue` — render SecondaryField from `secondaryCols` (computed over pool) instead of `secCount`

Let me also check the secondaryField test expectation around the "stays unbound until Phase D" test — it will need updating since we're now wiring it.
#### Step 1: Add pure helpers to src/utils/lrcParser.ts
#### Step 2: Create src/composables/useMerge.ts
Now I need to amend useAppState to add the `mainWarnText`, `mainWarnVisible` refs and augment the SecondaryEntry interface. Let me do that:
Now I need to remove the duplicate `SecondaryEntry` interface augmentation I had in useMerge.ts (since I just put it directly in useAppState.ts):
Now remove the `declare module` augmentation block I added to useMerge.ts since the interface is now defined in useAppState directly:
Now let me amend useAppState to add the `mainWarnText` + `mainWarnVisible` refs and the `useAppState()` return:
Now let me add `mainWarnText` and `mainWarnVisible` to the `useAppState()` return:
Now let me also fix the useMerge.ts — there's a leftover `_label` field that's not in the interface. Let me remove it:
Let me also remove the unused imports (`TS_RE`, `MAX_SECONDARIES`, `SecondaryEntry`):
`MAX_SECONDARIES` is used. `TS_RE` and `SecondaryEntry` aren't. Let me remove them:
Let me run vue-tsc to check progress:
I need to make the new fields optional or update all places that construct SecondaryEntry. The cleaner solution: make them required and fix the constructors. Let me find them all:
Let me add a helper `makeSecondaryEntry` for cleaner construction, and update all call sites. Actually the simpler fix is to make these optional via `?` since they have sensible defaults:
Also need to update useMerge.ts to handle undefined warnText/warnVisible. Let me also normalize entries when constructing:
Now let me also fix the `checkLineCounts` to handle the optional fields gracefully:
Also need to handle `entry.textareaEl` being possibly undefined in `syncScrollFrom`:
I accidentally deleted lines. Let me restore them:
Now let me also fix the `onSecInput` to handle the optional `textareaEl` and warn fields:
The cast is `as [number, number][]` but the type annotation is `[number, number | null][]`. Let me fix:
Types clean. Now let me wire SecondaryField.vue + EditorArea.vue + MenuBar.vue + App.vue.

#### Step 3: Wire SecondaryField.vue
#### Step 4: Wire EditorArea.vue — render SecondaryField from secondaryCols (pool)
Now update the EditorArea template — render SecondaryField from `secondaryCols`, and bind `#main-warn` reactively:
Wait — there's an issue with the SecondaryField `:key` + `:index`. The `secondaryCols` is a computed that returns `secondaryPool.value.filter(e => e.visible)`. The `:index` is `i + 1` (pool position 0 → "Secondary 1"). But the pool entry's position in `secondaryCols` may not match its position in `secondaryPool`. Actually it does — `secondaryCols` preserves the pool's order via `filter`, so the index `i` in `secondaryCols` corresponds to the pool index of that entry (because `filter` doesn't reorder).

Wait — that's NOT correct. If pool is `[A, B, C]` where B is hidden, `secondaryCols` = `[A, C]`. The `i` in `v-for` is 0 for A (pool index 0) and 1 for C (pool index 2). So `:index="i + 1"` would render "Secondary 1" for A (correct) but "Secondary 2" for C (wrong — should be "Secondary 3").

I need to use the actual pool index, not the secondaryCols index. Let me fix this by computing the pool index for each visible entry:

Actually, looking at the monolith more carefully — when addSecondary reuses a hidden entry, it sets the label to `Secondary ${visibleCount + 1}`. So the label is positional based on visible count, NOT based on pool index. The monolith's behavior is: when you hide field 2 and the pool is `[A, B(hidden), C]` with visible count = 2, the labels are "Secondary 1" (A) and "Secondary 2" (C). When you add a new field, B is reused and becomes "Secondary 3".

Hmm, actually that's a weird behavior. Let me re-read the monolith:

```js
const visibleCount=secondaryCols.length;
if(visibleCount<secondaryPool.length){
  const entry=secondaryPool[visibleCount];
  entry.col.style.display='';
  secondaryCols.push(entry);
  entry.lbl.textContent=`Secondary ${visibleCount+1}`;
  ...
}
```

So when reusing, it picks `secondaryPool[visibleCount]` — i.e., the pool entry at position `visibleCount` (not the next hidden one). And sets the label to `Secondary ${visibleCount+1}`. This means the labels are positional to the visible order, not to the pool order.

This is a weird design — the labels are positional to the visible column order, not to the pool entry's "identity". Let me match the monolith's behavior: `:index` = position in secondaryCols + 1.

But then the SecondaryField component needs to know which pool entry it represents. The `index` prop is 1-based visible position. The pool index is `index - 1` ONLY if the secondaryCols is built by `filter` and the visible entries are in pool order.

Actually wait — let me trace through the monolith more carefully:

1. Add field 1: pool = [A], cols = [A], label "Secondary 1"
2. Add field 2: pool = [A, B], cols = [A, B], label "Secondary 2"
3. Add field 3: pool = [A, B, C], cols = [A, B, C], label "Secondary 3"
4. Hide field: cols.pop() = C, C.col.style.display='none', cols = [A, B]
5. Add field: visibleCount=2 < pool.length=3, entry = pool[2] = C, C.col.style.display='', cols.push(C), label "Secondary 3"

So the label is `Secondary ${visibleCount+1}` = `Secondary 3` when reusing C. And the pool index of C is 2. So `visibleCount` (2) == pool index of C (2) in this case.

But what if we hide B instead?
1. Add field 1: pool = [A], cols = [A]
2. Add field 2: pool = [A, B], cols = [A, B]
3. Add field 3: pool = [A, B, C], cols = [A, B, C]
4. Can't hide B specifically — removeSecondary always pops the last visible. So cols = [A, B], C hidden.
5. Add field: visibleCount=2 < pool.length=3, entry = pool[2] = C, reused.

So in the monolith, you can only hide the LAST visible field, and the pool reuse always picks pool[visibleCount] which is the next hidden one in order. This means pool index == visible position for all visible entries. So `:index="i + 1"` where `i` is the position in `secondaryCols` IS the correct pool index.

But my `removeSecondary` finds "the last visible entry" — let me check:

```js
for (let i = secondaryPool.value.length - 1; i >= 0; i--) {
  if (secondaryPool.value[i]!.visible) {
    secondaryPool.value[i]!.visible = false
    ...
  }
}
```

This hides the last visible entry by pool index. So if pool is `[A, B, C]` all visible, it hides C (pool index 2). Then `secondaryCols` = `[A, B]`. Then `addSecondary` reuses the first hidden entry (C, pool index 2). The visible count is 2, so the label would be "Secondary 3" — matching pool index 2 + 1 = 3. 

So `:index="i + 1"` where `i` is the v-for index over `secondaryCols` is correct AS LONG AS the pool preserves order and hidden entries are always at the end. But what if the user hides field 2, then adds a new field? In the monolith, you can only hide the last visible — so this scenario doesn't happen.

But in my implementation, `removeSecondary` hides the last visible by pool index (scanning backwards). So the hidden entry is always the one with the highest pool index among visible entries. After hiding, `secondaryCols` = the remaining visible entries in pool order. So `i` in the v-for corresponds to the pool index of each visible entry — because `filter` preserves order and the hidden entry was the last one.

Wait, no. If pool = `[A, B, C]` all visible, and I hide C (pool index 2), then `secondaryCols` = `[A, B]`. The v-for index `i` is 0 for A (pool 0) and 1 for B (pool 1). `:index="i + 1"` = "Secondary 1" and "Secondary 2". Correct!

If I then add a new field, it reuses C (pool index 2, the first hidden). `secondaryCols` = `[A, B, C]`. The v-for index `i` is 0, 1, 2. `:index` = 1, 2, 3. Correct!

But there's a subtle issue: in my `removeSecondary`, I scan backwards from the end. If pool = `[A, B, C]` all visible, I hide C (pool index 2). But what if the user had previously hidden B, then re-added it, then the pool is `[A, B, C]` all visible but B was reused? The pool indices don't change — B is still at index 1. So the labels are still correct.

OK so `:index="i + 1"` is correct. But actually — the `index` prop in SecondaryField is used as `poolIdx = index - 1` to look up the entry. So `secondaryPool.value[poolIdx.value]` must be the right entry. Let me verify: if `secondaryCols` = `[A, B]` (pool indices 0, 1), and `:index="i + 1"` = 1, 2, then `poolIdx` = 0, 1. `secondaryPool.value[0]` = A, `secondaryPool.value[1]` = B. Correct!

But wait — what if the pool has hidden entries in the middle? E.g., pool = `[A, B(hidden), C]`, secondaryCols = `[A, C]`. The v-for index `i` is 0 for A (pool 0) and 1 for C (pool 2). `:index` = 1, 2. `poolIdx` = 0, 1. But `secondaryPool.value[1]` = B (hidden), NOT C! This is a bug.

Can this scenario happen? In the monolith, `removeSecondary` always pops the last visible, so hidden entries are always at the end of the pool. But in my implementation, `removeSecondary` scans backwards from the end — so it hides the last visible by pool index. Since visible entries are in pool order, the last visible is the one with the highest pool index. So hidden entries are always at the end.

But addSecondary reuses the FIRST hidden entry (not the last). So after hiding C (pool 2), addSecondary reuses pool[2] = C. Pool stays `[A, B, C]` all visible. No gap.

What if we hide C, then hide B? Pool = `[A, B, C]` with B and C hidden. secondaryCols = `[A]`. Then addSecondary reuses the first hidden = B (pool 1). secondaryCols = `[A, B]`. v-for `i` = 0, 1. `:index` = 1, 2. `poolIdx` = 0, 1. `secondaryPool.value[1]` = B. Correct!

Hmm, but what about this scenario:
1. Pool = [A, B, C] all visible
2. Hide C → secondaryCols = [A, B], C hidden (pool 2)
3. Hide B → secondaryCols = [A], B and C hidden (pool 1, 2)
4. Add field → reuse first hidden = B (pool 1). secondaryCols = [A, B]. v-for i = 0, 1. `:index` = 1, 2. poolIdx = 0, 1. `secondaryPool.value[1]` = B. Correct!
5. Add field → reuse next hidden = C (pool 2). secondaryCols = [A, B, C]. v-for i = 0, 1, 2. `:index` = 1, 2, 3. poolIdx = 0, 1, 2. Correct!

So as long as hidden entries are contiguous at the end of the pool, the `:index="i + 1"` approach works. But my `addSecondary` uses `find(e => !e.visible)` which picks the FIRST hidden — which may not be at the end.

Wait, actually, if I always hide from the end (scanning backwards), and I always reuse the first hidden, then the hidden entries are NOT necessarily contiguous at the end. Let me think again:

1. Pool = [A, B, C] all visible
2. Hide C → pool = [A, B, C(hidden)]
3. Add field → reuse C → pool = [A, B, C] all visible
4. Hide B → pool = [A, B(hidden), C] — NOT contiguous! B is hidden but C is visible.

After step 4: secondaryCols = [A, C] (filter preserves order). v-for `i` = 0 for A (pool 0), 1 for C (pool 2). `:index` = 1, 2. poolIdx = 0, 1. But `secondaryPool.value[1]` = B (hidden)! This is the bug.

Hmm, wait — can the user hide B specifically? In the monolith, `removeSecondary` always pops the LAST visible (cols.pop()). So you can only hide the field with the highest pool index among visible entries. In step 4, the last visible is C (pool 2), not B. So you CAN'T hide B while C is visible.

Let me re-check my `removeSecondary`:

```js
for (let i = secondaryPool.value.length - 1; i >= 0; i--) {
  if (secondaryPool.value[i]!.visible) {
    secondaryPool.value[i]!.visible = false
    ...
    return true
  }
}
```

This scans from the end and hides the first visible entry it finds scanning backwards — which is the visible entry with the highest pool index. So it matches the monolith's "pop the last visible" behavior. After hiding, hidden entries are always at the end.

So the scenario in step 4 (hide B while C is visible) CAN'T happen. 

But actually, there's still a subtle issue. If pool = [A, B, C] all visible, and I hide C (pool 2), then add a field (reuse C), then hide C again — the pool stays [A, B, C] with C toggled. But what if I add MORE fields?

1. Pool = [A, B, C] all visible (3 fields)
2. Hide C → pool = [A, B, C(hidden)] (2 visible)
3. Add field → reuse C → pool = [A, B, C] all visible (3 visible)
4. Add field → no hidden entry, push new D → pool = [A, B, C, D] all visible (4 visible)

This is fine. The labels are "Secondary 1", "Secondary 2", "Secondary 3", "Secondary 4". Pool indices 0, 1, 2, 3. Correct.

OK so the approach is correct. But I realize the `:index` prop should actually be the POOL INDEX + 1, not the v-for index + 1, because they might diverge if I ever change the hide/reuse logic. Let me make this more robust by passing the actual pool index.

Actually, the simplest robust approach: have SecondaryField receive the actual pool entry as a prop (or the pool index). Since `secondaryCols` is `secondaryPool.value.filter(e => e.visible)`, I need to map each visible entry back to its pool index. Let me just compute it:

Actually, let me simplify: I'll pass the pool index as a prop. To do that, I need to compute the pool index for each visible entry. The cleanest way is to NOT use `secondaryCols` (the computed) for the v-for, but instead iterate over `secondaryPool` and use `v-if="entry.visible"`:

Let me refactor EditorArea:
Now SecondaryField needs to handle the case where the entry may not be `visible` (it's rendered with `v-show`, so it's always mounted). Let me also update the SecondaryField script to not use `v-if="entry"` but always render:
Now wire MenuBar.vue — Add/Hide/Merge buttons:
Now update the template buttons:
Now wire App.vue — `setSyncCallbacks` for Tranche 6 + `setMergeCallbacks`:
Wait, I had `setSyncCallbacks` already imported from `useSync` in Tranche 5. Let me fix this naming clash — use the original import:
Now update the setSyncCallbacks call to wire Tranche 6 stubs to the real useMerge functions:
Also update the setMainText function to call checkLineCounts + updateMergeBtn (Tranche 6 stubs become real):
Now let me check the applySnapshot function (Tranche 5 had a stub) — Tranche 6 should add checkLineCounts + updateMergeBtn + clear extra secondaries:
Now let me run vue-tsc to check for errors:
Let me fix the aria-disabled type (should be boolean, not string):
And remove the unused `setMergeCallbacks` import from App.vue:
Types clean. Let me run the existing tests to check for regressions:
All 307 existing specs still pass. Now let me update the secondaryField test (the "stays unbound until Phase D" test needs to change since we're now wiring it):
Also update the menuBar test — the merge button test asserts `disabled` is defined. Now that the disabled state is computed, let me check it still passes:
Now let me write the useMerge test suite:
Let me look at the failures more carefully:
Issues:
1. `window.alert` is undefined in happy-dom — need to use `globalThis.alert` or stub differently
2. The merge test expects 6 lines but gets 7 — my counting was off (the trailing `\n` in mainText creates an extra empty line)
3. The paste test: the caret at position 6 of "before\nafter" — actually "before" is 6 chars, position 6 is right after "before" (before the \n). Let me recalculate: `ta.value = 'before\nafter'`, `selectionStart = 6`. Position 6 is between "before" and "\n". So `slice(0, 6)` = "before", `slice(6)` = "\nafter". The inserted text is "(pasted)\n(text)" (with paren wrap). Result: "before" + "(pasted)\n(text)" + "\nafter" = "before(pasted)\n(text)\nafter". That's 3 lines, not 4. My expectation was wrong.

Let me fix these:
Now replace all the `vi.spyOn(window, 'alert')` calls:
Now fix the mergeTranslations line count test (7 vs 6 — mainText has trailing \n):
Now fix the paste test expectation:
The `stubAlert()` returns a vi.fn() but `toHaveBeenCalledWith` isn't a function on it. Wait — that's weird, `vi.fn()` does have `toHaveBeenCalledWith`. Let me check what's happening:
The issue is `expect(alertSpy).toHaveBeenCalledWith(...)` — `alertSpy` is a plain vi.fn(), not the result of `expect()`. The correct form is `expect(alertSpy).toHaveBeenCalledWith(...)` — wait, that IS the correct form. Let me look more carefully...

Actually I see the issue — the `alertSpy` returned from `stubAlert()` is a `vi.fn()` but somehow `toHaveBeenCalledWith` isn't available. This might be because the spy was assigned via `Object.defineProperty` and lost the vi.fn() type. Let me try a different approach — use `vi.stubGlobal`:
Wait — there's one restore per test, but only 5 tests with alerts. Let me check:
Good — 5 tests with alerts, each has stub + restore. Let me run:
All 31 useMerge tests pass. Let me run the full suite:
339/339 specs green (was 307, +32 new for Tranche 6 — 31 useMerge + 1 secondaryField added). Now let me move to Tranche 7.

#### Tranche 7 — Import/Save

Let me re-read the monolith Import section (I already read it earlier) and create useImport.ts.
Now wire MenuBar.vue (Open/Save buttons) + App.vue (#file-picker ref + middle-click handler):
Now wire the Open/Save buttons in the template:
Now wire App.vue — register #file-picker ref, attach middle-click handler, wire useImport callbacks:
Now add the filePicker ref + register it + wire initImport callbacks. Let me find the right spot:
Now wire initImport callbacks after initMerge. Let me find the initMerge block:
Now I need to import `setupAudio` and `songTitle`/`songArtist` from useAudio/useTitle. Let me check the existing imports:
Now I need to extract `songTitle`/`songArtist` from useTitle:
Good — `songTitle` and `songArtist` are module-level refs. Let me extract them in App.vue:
Now I need to attach the middle-click + file-picker @change handlers in onMounted. Let me update onMounted:
Now I need to add `ref="filePicker"` to the #file-picker input in the template:
Now check that `setupAudio` is exported from useAudio:
Good. Let me run vue-tsc:
`mergeLrcMeta` lives in `pasteHandlers.ts` not `lrcParser.ts`. Let me fix:
Also fix the merge.test.ts unused `mainText`:
Types clean. Run tests:
All 339 specs still green. Now let me write the useImport test suite:
Two failures. Let me look at them:

1. `picker.value = '/fake/path/file.lrc'` — happy-dom's input element doesn't allow setting value to a non-empty string for file inputs. Let me skip that check or use a different approach.

2. The MAX_LINES test — the alert isn't being called. The issue is that `stubFileReaderText` sets `result` to the long text, but the test creates `lf` with the long text. The reader.onload fires and the code checks `raw.split('\n').length > MAX_LINES`. Let me check — `Array(501).fill('lyric').join('\n')` produces 501 lines (501 'lyric' joined by 500 newlines). `split('\n')` on that gives 501 elements. `501 > 500` is true. So the alert should fire.

Wait — the test might be failing because `alert` is called inside `reader.onload` which is called synchronously by my stub. But the alert spy was set up AFTER `stubFileReaderText`. Let me check the order:

```js
const alertSpy = stubAlert()
const longText = Array(501).fill('lyric').join('\n')
const restore = stubFileReaderText(longText)
```

The alert spy is set up first, then the FileReader stub. That should be fine. Let me check if `alert` is actually being called:
The issue: `stubAlert` uses `vi.stubGlobal('alert', fn)`, but the code calls `alert(...)` directly. `vi.stubGlobal` should replace `globalThis.alert`. Let me check if the useImport module captured `alert` at module load time (it didn't — it's called at runtime). Let me trace through:

Actually, `alert` is a global function. When useImport calls `alert(...)`, it resolves to `globalThis.alert`. `vi.stubGlobal('alert', fn)` should replace that. But maybe the issue is that the alert is being called inside a FileReader.onload callback that fires synchronously — and the stub was set up before the FileReader stub. Let me add some debugging:

Actually, looking more carefully: the test says `Number of calls: 0` — the alert was never called. This means either:
1. The line count check passed (didn't exceed 500) — but 501 > 500 should fail
2. The reader.onload didn't fire

Let me check `Array(501).fill('lyric').join('\n')` — this creates 501 'lyric' strings joined by 500 '\n'. So the result has 501 lines. `split('\n').length` = 501. `501 > 500` = true. The alert should fire.

Unless... the `normalizeLrcTimestamps` function transforms the text in a way that changes the line count? No, it just replaces `[mm:ss.cc\d]` with `[mm:ss.cc]` — doesn't add/remove newlines.

Wait — maybe the issue is that `stubFileReaderText` sets `result` via `Object.defineProperty`, but `normalizeLrcTimestamps` is called on `(ev.target?.result as string)`. Let me check if `ev.target` is the reader itself. In my stub:

```js
class MockReader {
  onload: ((ev: Event) => void) | null = null
  readAsText(_file: File) {
    Object.defineProperty(this, 'result', { value: text, configurable: true })
    if (this.onload) this.onload(new Event('load'))
  }
}
```

So `onload` receives `new Event('load')`. But the code reads `ev.target?.result`. In a real FileReader, `ev.target` is the reader itself. In my stub, `ev.target` is undefined (Event doesn't set target unless dispatched). Let me fix the stub:
Now fix the doImport test — happy-dom doesn't allow setting `.value` on file inputs:
All 12 useImport tests pass. Let me run the full suite + vue-tsc:
**351/351 specs green** (was 339, +12 new for Tranche 7) + `vue-tsc -b` clean.

Now let me update MEMORY.md + 0-Roadmap.md for Tranches 6 and 7:
Now update 0-Roadmap.md — mark Tranche 6 + 7 done + add impl notes:
Wait — I accidentally deleted the Tranche 5 entry from the list. Let me restore it:
Good. Now let me move to the **pre-push review pass**. Let me extract the Review Repomix:
I have the three audit skills. Let me read them:
Now let me audit my Tranche 6 + 7 code against these skills. Let me identify potential issues:

#### Pre-push review pass

Let me systematically audit the new code for:
1. **Aria-accessibility**: ARIA roles, accessible names, aria-hidden on focusable elements
2. **Code-quality**: CC (S3776), braceless-if (S2681), single source of truth, undo/redo model, config reads
3. **Sonarqube**: specific rule patterns

Let me check my new files for issues:
There are several **braceless-if** (S2681) violations — `if (x) return` / `if (x) continue` / `if (x) doThing()` without braces. Let me fix them. Per the code-quality skill: "Always add braces to single-line `if`/`else` bodies, even though JavaScript allows omitting them. SonarQube flags every instance."

Let me also check useSync.ts and useImport.ts:
There are many braceless-if violations across useMerge.ts, useImport.ts, and useSync.ts. These are S2681 violations. Let me fix them systematically. I'll start with useMerge.ts:
Let me fix these more carefully with replace_all for common patterns:
The whitespace might be different. Let me read the exact lines:
Ah — it's already fixed (I had a stale grep). Let me re-check the actual remaining braceless-ifs:
There are many. Let me write a script to fix them all at once using sed:
All braceless-ifs are fixed. But wait — my script might have over-matched and broken some lines. Let me verify with vue-tsc:
My script over-matched on a line where the "body" was an assignment with `+` continuation. Let me fix this manually:
Types clean. Let me run the tests to make sure the braceless-if fixes didn't break anything semantically:
351/351 still green. Now let me continue the audit. Let me check for other code-quality issues:

**Cognitive Complexity (S3776) — threshold 15**
Let me check the highest-risk functions for CC: `onFilePickerChange` (useImport.ts) has a 3-branch dispatch with nested FileReader callbacks:
`onFilePickerChange` has high CC — it has 3 branches, each with nested FileReader.onload callbacks that themselves have multiple `if`s + nested conditions. The `lrcOnly` branch's onload callback alone has ~6 `if`s + a nested `if` inside an `if` = CC ~8-9. The outer function adds the 3 dispatch branches + the onload callbacks. This likely exceeds CC 15 for the outer function (counting the callback as nesting).

The code-quality skill says to extract helpers. Let me extract the lrc-only + pair-import FileReader onload callbacks into named helpers. This is a legitimate refactoring that reduces CC:
Now replace the lrc-only + pair branches to use `processImportedLrc`:
Now let me run vue-tsc + tests:
Types clean + 351/351 green. Now let me audit the **aria-accessibility** concerns:

**Rule 2: Accessible name must contain visible label text**

Let me check the MenuBar buttons — the Add/Hide/Merge buttons have visible text "Add field" / "Hide field" / "Merge fields" but NO `aria-label`. Per the skill: "If visible text already describes the element, no `aria-label` is needed at all — the inner text is the accessible name." So these are fine.

The Open/Save buttons have `aria-label="Open file"` / `aria-label="Save"` but NO visible text (just emoji). Per Rule 8: "Every `<button>`, `<input>`, `<select>`, `<textarea>` needs an accessible name (via inner text, `aria-label`, or `aria-labelledby`)." The emoji 📂/💾 are visible but not text — so `aria-label` is correct. Per the S7927 guidance: icon-only buttons with `aria-label` are compliant (the emoji is not "visible label text" in the text sense).

Let me verify the SecondaryField still has accessible names:
Good. The textarea has `aria-label="Secondary ${index} lyrics"`, the file picker has `aria-label="Secondary ${index} lyrics file"`, the import button has `aria-label="Import secondary lyrics file"`. The 📂 button is icon-only with `aria-label` — compliant per Rule 8.

**Rule 3: Never put aria-hidden on focusable elements**

Let me check the `#file-picker` in App.vue:
That's `aria-hidden="true"` on a decorative SVG inside a button — that's fine (SVG is not focusable, the button is). Compliant per Rule 3.

**Rule 6: Dialog/modal accessibility**

The SettingsDialog uses shadcn-vue Dialog which handles this. Not touched this tranche.

**Code-quality checklist item 4: Undo/redo — single-push model**

Let me verify the `setMainText` function uses the wholesale-replacement pattern (pre + post) correctly:
The `setMainText` function does pre + post `pushSnapshot` — correct for wholesale replacement (per the code-quality skill). However, there's a subtle issue: `onSecInput` calls `scheduleSecInputSnapshot` (debounced, single push) which is correct for incremental edits. But `setMainText` is called by composables for wholesale replacement (import, paste, merge). The `onMainInput` handler (user typing in the main textarea) uses `setOnInputCallback` which calls `scheduleInputSnapshot` — also correct (single debounced push for incremental typing). 

But wait — `onMainInput` does NOT call `setMainText`. It directly sets `mainText.value = ta.value` and runs the side-effects WITHOUT the pre/post pushSnapshot. That's correct — user typing is incremental, so it uses the debounced single-push model. Good.

**Code-quality checklist item 5: Config reads**

Let me check that I'm not reading `DEFAULT_CFG` where I should read `cfg`:
Good — no hardcoded config reads. All reads go through `useAppState().cfg.value`.

**Code-quality checklist item 7: Existing Playwright test code does not conflict**

The Playwright tests target `docs/index.html` (the monolith) — they don't touch `src/`. No conflict possible this tranche.

**SonarQube-specific checks:**

Let me check for S6606 (prefer `Number.isNaN`):
No `isNaN` usage — good (the existing code already uses `Number.isNaN`).

Let me check for S7744 (useless `|| {}` after spread):
That's `target.files || []` — `target.files` is a `FileList | null`, not a spread. `Array.from(null)` throws, so the `|| []` is needed. Not an S7744 violation (the rule is about `|| {}` AFTER a spread, not before `Array.from`).

Let me check for S4138 (for-of conversion safety) — I have several `for` loops with index usage:
These all use the index `i` to index into arrays (`lines[i]`, `secondaryPool.value[i]`, etc.) — so for-of conversion would be unsafe per S4138. These are correctly left as `for` loops. The code-quality skill says: "When in doubt, keep the `for` loop."

Let me do a final vue-tsc + test run:
**351/351 green + vue-tsc clean.** 

#### Review summary

I audited the Tranche 6 + 7 code against the three review skills (aria-accessibility, code-quality, sonarqube-workflow) and fixed:

1. **S2681 (braceless-if)** — ~30 instances of `if (x) return` / `if (x) continue` / `if (x) doThing()` across `useMerge.ts`, `useImport.ts`, `useSync.ts` → all converted to braced `if (x) { … }` form.
2. **S3776 (cognitive complexity)** — `onFilePickerChange` in `useImport.ts` had a 3-branch dispatch with nested FileReader.onload callbacks, each with multiple conditions — extracted `processImportedLrc()` as a shared helper to keep `onFilePickerChange` under CC 15.

**No issues found in:**
- ARIA accessibility (icon-only buttons have `aria-label`, file pickers have `id` + `aria-label`, no `aria-hidden` on focusable elements, decorative SVGs inside buttons are fine)
- Single source of truth (secondaryPool is the authoritative ref, secondaryCols is a computed)
- Undo/redo model (`setMainText` uses pre + post for wholesale replacement; `onMainInput`/`onSecInput` use debounced single-push for incremental edits)
- Config reads (all go through `useAppState().cfg.value`, no `DEFAULT_CFG` reads at runtime)
- S6606 (already uses `Number.isNaN`), S7744 (no useless `|| {}` after spread), S4138 (for-loops correctly retained where index is used)

Now let me package the deliverables:
Now write the per-session deploy.sh:
`download/deliver.zip` is ready with 15 files (14 deliverables + `deploy.sh`), and `download/` is clean (only `deliver.zip`).

#### Summary

**Phase D Tranche 6 (Merge + Secondary fields) + Tranche 7 (Import/Save) + pre-push review pass** — all shipped.

**Tranche 6:**
- New `src/composables/useMerge.ts` — addSecondary/removeSecondary (pool management), checkLineCounts/updateMergeBtn (warn-bar + merge-button state), mergeTranslations (with `_buildMergedResult` verbatim), syncScrollFrom/syncSecScroll, onSecInput/onSecPaste/onSecKeydown/onSecFileImport handlers
- `lrcParser.ts` — added `getMainLyricLines` + `getSecLines` pure helpers
- `useAppState.ts` — `SecondaryEntry` augmented with `warnText`/`warnVisible`/`textareaEl`; added `mainWarnText`/`mainWarnVisible` refs
- `SecondaryField.vue` — full wiring: `:value` + `@input`/`@paste`/`@keydown`/`@scroll` + file picker + paren checkbox
- `MenuBar.vue` — Add/Hide/Merge buttons reactive (disabled state from pool + mergeDone + preconditions)
- `EditorArea.vue` — v-for over secondaryPool + `#main-warn` reactive + `#main-scroll` scroll sync
- `App.vue` — initMerge + applySnapshot secondary restore + setSyncCallbacks Tranche 6 stubs become real
- 31 new merge specs + 1 secondaryField added → 339/339

**Tranche 7:**
- New `src/composables/useImport.ts` — doImport, doSave (`[ti:]` filename + sanitise), onFilePickerChange (multi-file dispatch: audio-only/lrc-only/audio+lrc pair), onMiddleClick
- `MenuBar.vue` — Open/Save buttons wired
- `App.vue` — `#file-picker` ref + middle-click handler + initImport + onFilePickerChange listener
- 12 new import specs → 351/351

**Pre-push review pass** (aria-accessibility + code-quality + sonarqube-workflow skills):
- Fixed ~30 S2681 (braceless-if) violations across useMerge/useImport/useSync → all braced
- Fixed S3776 (cognitive complexity) in `onFilePickerChange` — extracted `processImportedLrc()` helper to keep the function under CC 15
- Verified: ARIA (icon-only buttons have aria-label, file pickers have id+aria-label, no aria-hidden on focusable), single source of truth (secondaryPool), undo/redo model (setMainText pre+post for wholesale, debounced single-push for incremental), config reads (all via `cfg.value`), no S6606/S7744/S4138 issues

**Final state:** 351/351 specs green + `vue-tsc -b` clean. `deliver.zip` (15 files) is in `download/`. Run `dpl` to deploy — then you can push and I'll review any issues that survive.

---
## Turn 4

### User:

One other issue before I push: `deploy.sh` cleans out *everything* in `scratch/`. It should instead only clean out items that were originally in the zip—see sonar-issue-exporter's `deploy.sh` for comparison.
```sh
=== Cleanup (collision prevention) ===
  rm: ./scratch.md
  rm: ./1789431616513564438.zip
```


**Attachments:**
`deploy-sie.sh`

---
### Agent:

I see the pattern. The sie `deploy.sh` uses a `.deliver-files.list` manifest that `unpack.sh` writes BEFORE extraction — it lists every file that came from the zip. The cleanup only removes files in that manifest, not `find . -maxdepth 1 -type f` (which sweeps pre-existing files like `scratch.md`).

Let me also check the sie `unpack.sh` to see how the manifest is written:
The existing linebyline `unpack.sh` does NOT write a `.deliver-files.list` manifest. I have two options:

1. **Update both `unpack.sh` AND `deploy.sh`** — unpack.sh writes the manifest before extraction, deploy.sh reads it. This is the sie pattern.
2. **Use the zip's own file list** — `deploy.sh` can run `unzip -l deliver.zip` to get the list, or better, use `zipinfo -1 deliver.zip` to get one filename per line. This doesn't require any `unpack.sh` changes.

Option 2 is cleaner — it doesn't require the user to also update `unpack.sh` (which is committed in the repo). Let me use that approach:
`zipinfo -1` gives one filename per line. This is the cleanest approach — `deploy.sh` reads the zip's own manifest, no `unpack.sh` changes needed. Let me also check if `unzip -Z1` is a portable alternative (in case `zipinfo` isn't installed):
`unzip -Z1` works and is guaranteed available (since `unzip` is already used by `unpack.sh`). Let me update the `deploy.sh` template to use this approach. I need to update both the per-session `deploy.sh` in `download/` AND the committed template explanation in MEMORY.md.

Let me first find the current `deploy.sh` in my workspace — I already overwrote it with `prepare.sh` (which zipped it into `deliver.zip`). Let me recreate it from the zip:
Now I need to fix the cleanup section. The problem is the `find . -maxdepth 1 -type f` approach sweeps everything in `scratch/`. Let me replace it with the `unzip -Z1` manifest approach:
Let me also read the full deploy.sh so I can write the fixed version:
Now let me write the fixed `deploy.sh`. The key change: replace the `find . -maxdepth 1 -type f` cleanup with a scoped approach that reads the zip's own file list via `unzip -Z1 deliver.zip`:
Now let me test the cleanup logic with a simulated scratch/ directory that has pre-existing files:
Wait — it STILL deleted the pre-existing files! But the script uses `unzip -Z1` now. Let me check — the issue is that `deploy.sh` I'm running is the OLD one (from the extracted zip). The new one I wrote to `/home/z/my-project/download/deploy.sh` wasn't in the zip yet. Let me re-test with the new deploy.sh:
Pre-existing files are preserved. But there's an issue — the `deliver.zip` was also extracted (the old `deliver.zip` from my test directory got cleaned up by the first `deploy.sh` run). Let me re-test properly — the `deliver.zip` should be in the scratch/ dir (it's what `unpack.sh` extracts). Let me simulate the real flow more accurately:
Wait — it STILL deleted the pre-existing files! But I'm using the new `deploy.sh`. The issue is that `deliver.zip` contains the OLD `deploy.sh` (with the `find . -maxdepth 1` approach). When I extracted `deliver.zip`, it overwrote my new `deploy.sh` with the old one!

Let me trace through:
1. `cp /home/z/my-project/download/deliver.zip .` — this is the OLD zip with the OLD deploy.sh
2. `unzip -oqq deliver.zip` — extracts the OLD deploy.sh, overwriting nothing (there was no deploy.sh yet in scratch)
3. `bash deploy.sh` — runs the OLD deploy.sh (from inside the zip), not my new one

I need to rebuild `deliver.zip` with the new `deploy.sh` first. Let me re-prepare:
Now let me re-test with the new `deliver.zip` (which contains the fixed `deploy.sh`):
The fix works:
- **✓ `scratch.md` preserved** (pre-existing file NOT deleted)
- **✓ `1789431616513564438.zip` preserved** (pre-existing file NOT deleted)
- **✓ All zip-extracted files moved/deleted** (src-App.vue, MEMORY.md, etc. — all deployed to the repo)

The cleanup section now reads the zip's own file list via `unzip -Z1 deliver.zip` and only removes files that came from the zip — pre-existing files in `scratch/` are untouched. The "scoped to zip-extracted files" message confirms the new behavior.

Let me also update MEMORY.md to document this fix + update the committed template description:
Now let me also update the MEMORY.md deploy pattern section to reference the fix:
I see there's already a MEMORY.md entry at line 269 that says the cleanup was already replaced with `known_zip_files=()`. But that was for the OLD `deploy.sh` template (the committed one). My per-session `deploy.sh` was using `find . -maxdepth 1 -type f` anyway. Let me update the deploy pattern description at line 181 to note the fix:
Now rebuild deliver.zip with the updated MEMORY.md + fixed deploy.sh:
The `deploy.sh` was cleaned up by the previous prepare.sh run. Let me re-copy it:
Let me re-write the fixed deploy.sh to download/:
Now let me run the final end-to-end test with the new deliver.zip:
The fix works correctly:
- **✓ `scratch.md` preserved** — pre-existing notes untouched
- **✓ `1789431616513564438.zip` preserved** — pre-existing upload untouched
- **✓ All 14 zip-extracted files deployed** to the repo (none left in scratch/)
- **✓ Cleanup message** says "scoped to zip contents (pre-existing files preserved)"
Fixed. The `deploy.sh` cleanup now uses `unzip -Z1 deliver.zip` to read the zip's own file manifest — it only removes files that came from the zip, leaving pre-existing `scratch/` files like `scratch.md` and old upload zips untouched. Verified with a full end-to-end simulation: `scratch.md` + `1789431616513564438.zip` both survive the deploy.

`deliver.zip` is rebuilt with the fixed `deploy.sh` + updated MEMORY.md (new bug-pattern entry documenting the `find . -maxdepth 1` vs `unzip -Z1` fix). Ready to push.