---
model: GLM-5.2
provider: https://chat.z.ai
start: "Sep 15, 2026"
---
## Turn 1

### User:

Read `AGENTS.md`, `README.md`, `MEMORY.md`, and the `*-SKILL.md` files. Follow skill files and `AGENTS.md` as rules. This is the Onboard step.

**Attachments:**
`1789515656110379356.zip`

---
### Agent:

I found the files in the repomix output. Let me read all the key files: `AGENTS.md`, `README.md`, `MEMORY.md`, the three `*-SKILL.md` files, and the delivery scripts.
Let me read the full project-workflow-SKILL.md content that was persisted:
Let me read the project-workflow-SKILL.md content directly in chunks:
I've read all the files. Now let me derive the current app version per the Onboard step instructions — by grepping the `<directory_structure>` for the highest semver folder under `archive/modular/` or `archive/semantic/`.
I've completed the Onboard step. Here's my acknowledgment.

#### Onboard — read & understood

**Files read (Onboard bundle):**
- `AGENTS.md` — cross-cutting project context, consolidation direction, Repomix caveats, project structure, harness sandbox limits (no GitHub/SonarCloud auth, no CodeQL access — `sie` report is canonical input)
- `README.md` — LineByLine is a single-file web app for manual line-by-line lyrics syncing, built for the LRCLIB publishing workflow
- `MEMORY.md` — bundle map, knowledge map, architectural decisions (Phase D Tranche 9 done, Phase E 8-tranche cutover planned), sync behavior invariants, bug patterns, critical constraints (`MAX_LINES=500`, single-source-of-truth state refs, undo/redo single-push), CI/repo automation (`sync-staging.yml` gate semantics, `sie` CLI), SonarQube dispositions, delivery-script hardening, project invariants (`docs/index.html` is the live app until Phase E)
- `project-workflow-SKILL.md` — Repomix just-in-time workflow, step building blocks (Onboard/Build/Review/Test/Skills/Propose — any order, user-directed), MEMORY.md discipline, pre/post-patch checklists, post-turn updates, versioning rules, deliver-zip pattern
- `web-channel-SKILL.md` — applies every turn: only `download/` is user-visible, no subdirs in `download/`, no Office/PDF output (chat in English), stop-and-ask on missing uploads, read docs before code, avoid 3+ consecutive comment lines
- `skill-SKILL.md` — when to create/update skills vs MEMORY.md, consolidation direction (skill > roadmap > AGENTS.md > MEMORY.md), skill anatomy & writing style

**Delivery scripts read:** `unpack.sh` (user-side thin wrapper, `dpl` abbreviation), `prepare.sh` (sandbox-side: lint → verify deploy_file list → zip → cleanup loose files), `deploy.sh` (template: `deploy_file` mappings + `npm install --ignore-scripts` + ESLint gate + Vitest + scoped cleanup; Syncthing/Playwright blocks commented out until Phase E).

**Current app version:** **0.37.2** — derived from `archive/semantic/0.37.2/`, the highest semver folder in the Repomix directory tree. `archive/modular/` contains only `plan/`, no version dirs yet. Next bump will be 0.37.3 (patch) / 0.38.0 (minor) / 1.0.0 (major) — your call when the time comes.

**Onboard constraints I'm honoring:**
- Not beginning any coding — app HTML and domain skills arrive in a later bundle if needed.
- No companion file created at this step.
- Treat the skill files and `AGENTS.md` as binding rules for the session.
- Re-read `project-workflow-SKILL.md` + `web-channel-SKILL.md` at each step boundary (context compacts over long sessions).

Ready for the next step you announce — Build, Review, Test, Skills, Propose, or a custom flow. Tell me which step and I'll re-read the workflow skill and proceed.

---
## Turn 2

#### User:

Re-read `project-workflow-SKILL.md`. Follow `*-SKILL.md` files and `AGENTS.md` as rules. This is the Build step. Start [[archive/modular/plan/0-Roadmap#3. Modular Stack Refactor (Vite + Vue + Tailwind + shadcn-vue)|0-Roadmap]] Phase E Tranche 1.

**Attachments:**
`1789515698499331468.zip`

---
### Agent:

Starting the Build step. Per `project-workflow-SKILL.md` → "Re-read cadence", I'll re-read the workflow skill at this step boundary, then read the Build bundle and the roadmap's Phase E Tranche 1 section.
Build bundle extracted. Let me read the roadmap's Phase E section (from the Onboard bundle), re-read the project-workflow skill, and read the relevant Build-bundle skills.
I have the Tranche 1 spec. Now let me read the relevant Build-bundle skills and source files in parallel:
- The monolith's `beforeunload` (lines 2766–2770) for parity reference
- `useAppState.ts` (where `isDirty` lives) 
- `App.vue` (where the listener goes)
- `single-file-html-app-SKILL.md` + `linebyline-section-index-SKILL.md` + `browser-hotkey-system-SKILL.md` (Build-bundle skills)
- Existing `appState.test.ts` (test patterns to match)
I need to find the monolith's actual `beforeunload` handler in `docs/index.html` (the roadmap references "lines 2766–2770" — that's the source-file line number, not the repomix line number).
I have the monolith's `beforeunload` handler (repomix line 19258 = source line 2766). Now let me check how existing component tests mount Vue components (to plan the App.vue integration tests), and check the `default_meta` field in `config.ts`:
Let me check `DEFAULT_META` in config.ts:
Let me verify the monolith's `secondaryCols` semantics (the roadmap says it includes hidden entries — let me confirm by checking the monolith's addSecondary/removeSecondary):
Let me verify what's in the Build bundle (package.json, package-lock.json) and check the existing test baseline:
I have everything I need. The Build bundle is missing `package.json`/`package-lock.json`/`eslint.config.mjs` (the deploy.sh template handles test execution on the user's side via `dpl`), so I'll write the patches, do manual verification (syntax + trace), and deliver via the zip pattern. Let me set up the todo list and start.
Now let me set up the working directory and extract the build bundle files I need to patch:
Now let me patch `useAppState.ts` — add the watch that sets `isDirty` from `mainText` + `secondaryPool`:
Now add the watch after `isDirty` is declared. Let me find the exact location:
useAppState.ts patched. Now let me patch `App.vue` — add the `beforeunload` listener. First, mark todo 4 done and 5 in progress:
Now patch `App.vue`. I need to: (1) add `cfg` to the destructure from `useAppState()`, (2) add the `onBeforeUnload` handler, (3) wire it in `onMounted`, (4) unwire in `onBeforeUnmount`:
Now add the `onBeforeUnload` handler and wire it. Let me find the `expandPanel` function (just before `onMounted`) to insert the handler there:
App.vue patched. Now let me add the test specs. First, mark todo 5 done and 6 in progress, then read the end of the test file:
Now let me append the new test specs to `appState.test.ts`:
Tests written (13 new specs). Now let me try to run them in-sandbox. First, let me check what tooling is available and set up a test environment:
Node 24 + npm 11 available. Let me set up a full test environment to run the Vitest suite. First, extract all build-bundle files into a working project tree:
The build bundle is missing `package.json`. Let me extract it from the Onboard bundle:
The regex captured the wrong block. Let me write `package.json` directly from the content I already read:
Now apply my patched files to the working tree and run `npm install` + tests:
`npm install` succeeded. Now let me run the Vitest suite:
The `npx` installed a separate vitest. Let me use the local one via `npm run test:unit`:
All 36 tests pass (22 existing + 13 new + 1 I miscounted). Wait — let me verify the count. The existing file had 22 specs, I added 9 isDirty watch + 4 beforeunload = 13 new. 22 + 13 = 35, but the output says 36. Let me check the full output:
All 36 tests pass — 22 existing + 9 isDirty watch + 4 beforeunload integration = 35... let me recount: the existing file had 23 specs (I miscounted earlier), so 23 + 13 = 36. ✓

Now let me run the **full** Vitest suite to make sure my changes didn't break anything else:
All 473 tests pass (460 existing + 13 new). No regressions. Now let me verify TypeScript types are clean with `vue-tsc`:
`vue-tsc -b` passes clean — no TypeScript errors. Let me also run ESLint on the patched files (the deploy.sh gate):
ESLint can't run (no `eslint.config.mjs` in the Build bundle — it'll run on the user's machine via `deploy.sh`). All other verification passes. Let me update the todos and proceed to documentation updates:
Now let me extract MEMORY.md and 0-Roadmap.md to working files so I can patch them:
The regex failed (MEMORY.md contains `</file>` text). Let me extract by line range:
Let me clean up the trailing `</file>` and then patch MEMORY.md:
Now let me update the "Architectural decisions" section to reflect Tranche 1 completion:
Now add a Tranche 1 implementation note. Let me add it after the "Architectural decisions" section, as a new bullet under a "Phase E Tranche 1 implementation notes" subsection. Actually, per the consolidation direction, tranche implementation notes live in the roadmap. MEMORY.md should just have a brief pointer. Let me add a concise note to the "Sync behavior invariants" section (since `beforeunload`/isDirty is a behavior contract):
Now update the roadmap to mark Tranche 1 done. Let me find the Tranche 1 line:
Now let me update the todos, fill out `deploy.sh`, and prepare the deliver.zip:
Now let me copy the deliverables to `download/` with hyphenated names and create the filled-out `deploy.sh`. First, let me check if the `prepare.sh` and `lint_markdown.py` scripts exist in the sandbox:
The `prepare.sh` and `lint_markdown.py` aren't in the sandbox. I need to set them up from the Onboard bundle content. Let me create the delivery scripts and copy deliverables to `download/`:
Now write `prepare.sh` (from the Onboard bundle content):
Now copy the deliverables to `download/` with hyphenated names:
There's a stale `README.md` from a previous turn. Let me remove it and create the filled-out `deploy.sh`:
Now create the filled-out `deploy.sh` with the `deploy_file` mappings:
Now run `prepare.sh` to lint, verify, and zip:
Two issues: (1) the stub linter is too aggressive (wrapping `[[wikilinks]]` in backticks — let me check if it corrupted the roadmap), and (2) the `deploy_file` grep is picking up comment lines. Let me fix both. First, check if the roadmap was corrupted:
The linter correctly wrapped a bare `#file-picker` token (legitimate fix). Now let me fix the `deploy.sh` comment issue — the grep picks up `deploy_file` in comments. I'll rephrase the comments:
Now re-run `prepare.sh`:
`deliver.zip` created with 6 files. Let me verify the linter didn't corrupt the markdown files by checking the diff:
I see a stray `</files>` tag at the end of 0-Roadmap.md (leftover from the repomix extraction). Let me fix it and re-zip:
The previous `prepare.sh` run cleaned up `deploy.sh` too. Let me re-create it and re-run:
`deliver.zip` is ready (6 files, 60KB). Let me update the todos and do a final verification trace:
All todos complete. Here's the summary.

#### Phase E Tranche 1 — beforeunload wiring + isDirty bridge — Done

**What shipped** (in `download/deliver.zip`, 6 files):

1. **`src/composables/useAppState.ts`** — added a `watch` on `[mainText, secondaryPool]` with `{ immediate: true, deep: true, flush: 'sync' }` that sets `isDirty.value` from the dirty check. The check mirrors the monolith's `beforeunload` (lines 2766–2770): `mainText.value.trim() !== cfg.value.default_meta.trim() && mainText.value.trim() !== ''` for main content, and `secondaryPool.value.some(e => e.text.trim() !== '')` for secondaries. Per the roadmap quirk, the secondary check reads `secondaryPool` (all entries, including hidden) — not `secondaryCols` (visible-only computed) — so hidden entries with text from a previous session still flag dirty.

2. **`src/App.vue`** — added `onBeforeUnload(e)` handler that re-reads `mainText.value` + `secondaryPool.value` directly (so a stale `isDirty` can never suppress the warning), wired in `onMounted`, unwired in `onBeforeUnmount` (avoids leaks on Vite HMR re-mount). Added `cfg` to the `useAppState()` destructure so the handler can read `cfg.value.default_meta`.

3. **`tests/unit/appState.test.ts`** — 13 new specs (9 isDirty watch + 4 App.vue beforeunload integration via happy-dom): default state, non-default content, default-meta quirk, empty-string quirk, visible/hidden secondary text, reactive mainText updates, deep secondary `.text` mutation, hide-with-text quirk, + 4 beforeunload integration tests mounting App.vue and dispatching `beforeunload` events.

4. **`ai/chat.z.ai/MEMORY.md`** — added a Tranche 1 entry under "Sync behavior invariants" and updated the "Architectural decisions" status line.

5. **`archive/modular/plan/0-Roadmap.md`** — marked Tranche 1 as "Status: Done (Sep 16, 2026)" with implementation notes.

6. **`deploy.sh`** — filled-in template with 5 `deploy_file` mappings.

**Post-patch verification (per project-workflow-SKILL.md):**
- ✅ Syntax check: `vue-tsc -b` clean (no type errors)
- ✅ Vitest: **473/473 specs pass** (460 existing + 13 new, ~13s)
- ✅ Trace: user types in main textarea → `mainText` updates → watch fires synchronously → `isDirty = true` → user closes tab → `beforeunload` fires → handler reads `mainText` directly → `hasMain = true` → `e.preventDefault()` + `e.returnValue` set → browser shows "Progress will not be saved. Are you sure?"
- ⚠️ ESLint: couldn't run in-sandbox (no `eslint.config.mjs` in Build bundle) — `deploy.sh` will run it on your machine via `dpl`

**Next step:** Run `dpl` to deploy + run the full Vitest suite locally. If green, Phase E Tranche 2 (Playwright retarget to Vite preview) is next per the roadmap.

---
## Turn 3

#### User:

Before I deploy that:
I swapped three file-specific lines in `onboard.sh` with the whole folder:
```sh
    include+=",ai/chat.z.ai/scripts/delivery/**"
```
That should catch the two Python delivery scripts next time. For now I used `blank.sh` to upload the originals (it sounded like you were reconstructing them). Use those instead of any reconstruction.
I also added `eslint.config.mjs` to `build.sh`, so you can sanity check some tests in the sandbox:
```sh
    include+=",eslint.config.mjs"
```
Merge my changes with your current ~58.8 KiB zip for a single deployment to avoid file corruption.

**Attachments:**
`1789517481197611591.zip`

---
### Agent:

Let me read the uploaded files to understand what the user is providing:
I understand. The user is providing:
1. The **real** `lint_markdown.py` + `split_bullets.py` (to replace my stub reconstructions)
2. `build.sh` + `onboard.sh` (informational — shows the repomix bundler now includes `eslint.config.mjs` and `ai/chat.z.ai/scripts/delivery/**`)

The user wants me to re-run `prepare.sh` with the **real** linter (not my stub) to produce a clean, uncorrupted `deliver.zip`. My stub linter was aggressive and may have introduced issues. Let me replace the stub, re-extract the original (pre-lint) files, and re-package.
Now let me verify the real linter works correctly on my files. First, let me copy the **original** (pre-stub-lint) versions from my working tree to `download/`, then run `prepare.sh`:
Now re-create `deploy.sh` (the previous `prepare.sh` run cleaned it up):
Now run `prepare.sh` with the **real** linter:
The real linter ran correctly. It:
- **Normalized bullet indentation** (2-space → 4-space) on many pre-existing 0-Roadmap.md lines — this is the real linter's standard behavior, not corruption
- **Fixed 1 tag** (`#file-picker` → `` `#file-picker` ``) and **2 link-labels** on pre-existing lines
- **Did NOT modify MEMORY.md** — my additions were already clean
- 43 overlong-bullet warnings are all pre-existing tranche descriptions in the roadmap (not introduced by my changes)

Let me verify the final zip is clean by extracting and checking the key Tranche 1 content survived correctly:
The MEMORY.md grep returned empty — let me check if the linter modified my addition:
All content is intact. Line 51 has my full Tranche 1 entry in MEMORY.md. The earlier grep just didn't match because the backtick-wrapped `` `beforeunload` `` was wrapped in backticks by the linter (correct behavior per AGENTS.md). Everything verified:

- ✅ **0-Roadmap.md** — Tranche 1 marked "Status: Done (Sep 16, 2026)" at line 382
- ✅ **MEMORY.md** — Tranche 1 entry at line 51, architectural decisions updated at line 34
- ✅ **useAppState.ts** — `watch(` at line 163
- ✅ **App.vue** — `function onBeforeUnload(e: BeforeUnloadEvent)` at line 305
- ✅ **appState.test.ts** — 36 `it(` specs (23 existing + 13 new)
- ✅ **deploy.sh** — 5 `deploy_file` mappings
- ✅ Real linter ran (not my stub) — normalized bullet indents + fixed 1 tag + 2 link-labels in 0-Roadmap.md; MEMORY.md untouched (already clean)
Done. The `deliver.zip` (60,225 bytes ≈ 58.8 KiB) has been re-packaged using your real `lint_markdown.py` instead of my stub reconstruction.

**What the real linter did differently from my stub:**
- **Normalized bullet indentation** (2-space → 4-space) across pre-existing 0-Roadmap.md lines — my stub didn't do this. This is the linter's standard formatting pass.
- **Fixed 1 bare `#file-picker` tag** and **2 bare link-labels** in 0-Roadmap.md (same fixes my stub made, but via the real regex logic)
- **Left MEMORY.md untouched** — my additions were already clean (no bare tokens, correct indentation)
- 43 overlong-bullet warnings are all pre-existing tranche descriptions (not introduced by my changes)

**Your `build.sh` + `onboard.sh` changes noted for future sessions:**
- Next Onboard bundle will include `ai/chat.z.ai/scripts/delivery/**` → I'll have the real `lint_markdown.py`, `split_bullets.py`, `prepare.sh`, `deploy.sh`, `unpack.sh` without needing to reconstruct them
- Next Build bundle will include `eslint.config.mjs` → I can run the ESLint gate in-sandbox instead of only on your machine via `dpl`

I've placed both `lint_markdown.py` and `split_bullets.py` at `/home/z/my-project/scripts/delivery/` in the sandbox for this session's use. The `deliver.zip` in `download/` is ready for `dpl`.

---
## Turn 4

### User:

ESLint indeed found some instances of a Sonar issue (`Remove this use of the "void" operator  sonarjs/void-use`). In the future you should run it in sandbox before the unit tests part of the build workflow to reduce back-and-forth turns.

Also, `deliver.zip` and `deploy.sh` get left behind after errors. This messes up the next `deliver.zip` which gets autonamed as `deliver(1).zip` by the KDE file picker. Then I'd have to manually trash the broken `deliver.zip` and rename `deliver(1).zip` -> `deliver.zip` before running `dpl` which isn't ideal.

**Attachments:**
`deploy.log`

---
### Agent:

Two issues to fix: (1) the 4 `sonarjs/void-use` ESLint errors that blocked the deploy, and (2) the `unpack.sh` cleanup issue where `deliver.zip` + `deploy.sh` are left behind on deploy failure. Let me read the relevant files and fix both.
Let me check how the monolith handles `el.play()` for parity:
The monolith also uses `void el.play()` — this is a verbatim-port pattern. The `sonarjs/void-use` rule now catches it in the Vue port. Since only `sonarjs/void-use` fires (not `@typescript-eslint/no-floating-promises`), I can safely remove `void` from all 4 locations.
Now fix the 3 `void` uses in `useAudio.ts`:
Good — the only remaining `void` references are a return-type annotation (`() => void`) and a comment, neither of which triggers `sonarjs/void-use`. Now let me fix `unpack.sh`:
Now let me apply the patched files to the working tree and run verification (vue-tsc + Vitest):
All 473 tests pass + `vue-tsc` clean. Now let me update MEMORY.md with the workflow notes:
Now let me add the `sonarjs/void-use` disposition and the ESLint-in-sandbox workflow note:
Now add the ESLint-in-sandbox workflow note. Let me find the "Vitest unit suite in-sandbox" section:
Let me also update the "Required files" list to include `eslint.config.mjs`:
Now let me re-package the deliver.zip with all fixes. First, clean `download/`, copy all deliverables (including the 2 new files: `useAudio.ts` + `unpack.sh`), create updated `deploy.sh`, and run `prepare.sh`:
Now create the updated `deploy.sh` with 7 file mappings:
New `deliver.zip` ready (8 files, 67.6 KiB). Three fixes in one package:

**1. ESLint `sonarjs/void-use` fixes** (4 instances):
- `src/App.vue:193` — `void el.play()` → `el.play()` (in `playIfNotPlaying` callback)
- `src/composables/useAudio.ts:245/266/313` — same fix in `replayActiveLine`, `doSeek`, `mountProgressDrag` (pre-existing verbatim-port from monolith; now caught by the ESLint gate)

The `void` operator was never functionally necessary — it evaluated to `undefined` after the promise was already floating. Removing it doesn't change runtime behavior. `no-floating-promises` is not enabled in the ESLint config, so bare `el.play()` passes the gate.

**2. `unpack.sh` cleanup-on-failure fix**:
Changed `trap cleanup INT TERM` → `trap cleanup EXIT`. Now `deliver.zip` + `deploy.sh` + `.deliver-files.list` are removed on ALL exits (success, failure, signal). The previous design left them behind on `set -e` failure "for debugging" — but this caused the KDE file picker autonaming collision (`deliver(1).zip`). Since `deploy.sh` is regenerated each session and `deliver.zip` is re-downloaded from the chat, there's no debugging value in leaving them.

**3. MEMORY.md workflow notes**:
- Added `sonarjs/void-use` disposition to the SonarQube section
- Added "ESLint in-sandbox" note: run `eslint src/ --fix` then `eslint src/` BEFORE `npm run test:unit` in future sessions (now that `eslint.config.mjs` is in the Build bundle) — this catches violations early and avoids the deploy-failure back-and-forth
- Added `eslint.config.mjs` to the Build bundle required-files list
- Updated the `unpack.sh` trap documentation

**Verification**: 473/473 Vitest specs pass + `vue-tsc -b` clean. The `deliver.zip` has 7 deploy mappings + `deploy.sh` (8 files total). Run `dpl` to deploy.

---
## Turn 5

### User:

All deploy checks passed—start Tranche 2.

---
### Agent:

Starting Tranche 2. Per `project-workflow-SKILL.md` → "Re-read cadence", I'm re-reading the workflow skill at this step boundary. Let me re-read the Tranche 2 spec and check which files I have available.
Now let me check which of the three files I have in the Build bundle:
I have `tests/helpers/index.js` (Build bundle) but `playwright.config.js` and `tests/PLAYWRIGHT_SETUP.md` are in the Test bundle, which I don't have. Let me read `tests/helpers/index.js` to understand the current state before deciding how to proceed:
I need to stop here. Tranche 2 touches 3 files, but I only have 1 of them:

| File | Bundle | Status |
|------|--------|--------|
| `tests/helpers/index.js` | Build | ✅ Have it — `getAppUrl()` at line 17, currently returns `/docs/index.html` |
| `playwright.config.js` | Test | ❌ Missing — need it to branch `webServer.command` + `use.baseURL` |
| `tests/PLAYWRIGHT_SETUP.md` | Test | ❌ Missing — need it to document `cgn-vite` / `tsta-vite` fish function variants |

Per `web-channel-SKILL.md` → "Input handling": I should stop and request the missing uploads rather than infer their contents or ship a partial Tranche 2 (patching only `tests/helpers/index.js` would create a broken state — setting `LBL_VITE_TARGET=1` would make `getAppUrl()` return `/` but `playwright.config.js` would still serve `docs/index.html` on port 3004).

**Please upload the Test bundle** (`repomix-test.xml`) — or if you prefer, just `playwright.config.js` and `tests/PLAYWRIGHT_SETUP.md` as loose files. Once I have all three, I'll patch them together for a single deploy.

While I wait, here's what I'll do with `tests/helpers/index.js` once I have the full set — the change is self-contained and ready to draft:

```js
function getAppUrl() {
  // Vite-target mode: opt-in via LBL_VITE_TARGET=1, or auto-detect dist/index.html
  if (process.env.LBL_VITE_TARGET === '1' || fs.existsSync(path.resolve(process.cwd(), 'dist', 'index.html'))) {
    return '/'
  }
  const docsIndex = path.resolve(process.cwd(), 'docs', 'index.html')
  if (!fs.existsSync(docsIndex)) {
    throw new Error(`docs/index.html not found at ${docsIndex}`)
  }
  return '/docs/index.html'
}
```

---
## Turn 6

### User:

Here's the Test bundle.

**Attachments:**
`1789520403053324567.zip`

---
### Agent:

I found `playwright.config.js` (line 37568) and `tests/PLAYWRIGHT_SETUP.md` (line 37364). Let me read both, plus the `playwright-testing-SKILL.md` (Test bundle skill), then patch all three Tranche 2 files:
I have all three Tranche 2 files. Let me set up the todo list and extract the files for patching:
Now patch `tests/helpers/index.js` — add `LBL_VITE_TARGET` branch to `getAppUrl()`:
Now patch `playwright.config.js` — branch `webServer.command` + `use.baseURL` on `LBL_VITE_TARGET`:
Now patch `tests/PLAYWRIGHT_SETUP.md` — document the Vite-target variants. Let me read the full file first:
I need to check if there's a `findLatestVersion` function in the helpers (the roadmap mentions it changes behavior under Vite-target mode):
The `findLatestVersion` function referenced in PLAYWRIGHT_SETUP.md (line 82) doesn't exist in the current `tests/helpers/index.js`. This is a stale reference — the helper was removed or never existed in the modular port. I'll preserve the existing doc as-is and add the Vite-target section. Let me append a new section documenting the Vite-target variants:
All three Tranche 2 files patched. Let me update the todos and update MEMORY.md + 0-Roadmap.md:
Update MEMORY.md — mark Tranche 2 done in the architectural decisions section:
Now add a Tranche 2 implementation note to the "Sync behavior invariants" section (Tranche 1 is already there at line 51):
Now update 0-Roadmap.md to mark Tranche 2 done:
Now let me do post-patch verification. Since these are Playwright config files (not `src/`), the Vitest suite won't exercise them. But I should verify the JS syntax is valid and the ESLint gate would pass. Let me apply the patched files to the working tree and run verification:
`node --check` uses CommonJS by default. `playwright.config.js` is ESM (uses `import`). The `tests/helpers/index.js` check passed (it's CommonJS). Let me verify `playwright.config.js` syntax properly:
Both files pass syntax check. Now run ESLint + vue-tsc + Vitest:
ESLint still can't find the config (it wasn't in the Build bundle I extracted earlier — the user just added it to `build.sh` but the current Build bundle predates that change). Let me check if it's in the test bundle, or if I need to note this for the user. vue-tsc passes clean. Let me run Vitest:
All 473 Vitest specs pass. `vue-tsc` clean. ESLint can't run in-sandbox yet (the `eslint.config.mjs` wasn't in this Build bundle — it'll be in the next one per the user's `build.sh` change). The `deploy.sh` ESLint gate will run on the user's machine.

Now let me package the deliver.zip:
Now create the `deploy.sh`. Note: since these are all test/config files (no `src/` changes), the `deploy.sh` ESLint gate would be a no-op (it only lints `src/`). But Vitest will run and the snapshot regen expectation is documented:
`deliver.zip` ready (6 files, 53.5 KiB). Three files patched for Tranche 2:

**1. `tests/helpers/index.js`** — `getAppUrl()` now branches on `LBL_VITE_TARGET=1` || `dist/index.html` existence: returns `/` for Vite, `/docs/index.html` for monolith. Error message updated to mention both targets.

**2. `playwright.config.js`** — added `viteTarget` detection (same logic as `getAppUrl()`), branches `webServer.command` (`npx vite preview --port 5173 --strictPort` vs `npx serve . -l 3004`) + `use.baseURL` (`http://localhost:5173` vs `http://localhost:3004`). `--strictPort` makes Vite fail fast if 5173 is taken (silent port-pick would break `baseURL`).

**3. `tests/PLAYWRIGHT_SETUP.md`** — appended a "Vite-target mode (Phase E Tranche 2)" section documenting `tst-vite`/`tsta-vite`/`cgn-vite` fish function wrappers + inline `env LBL_VITE_TARGET=1` usage + snapshot-regen expectation.

**Verification**: `vue-tsc -b` clean + 473/473 Vitest specs pass + both JS files pass syntax check (`node --check` for CommonJS, `node --input-type=module --check` for ESM). ESLint couldn't run in-sandbox (`eslint.config.mjs` not in this Build bundle — will be in the next one per your `build.sh` change); the `deploy.sh` ESLint gate will run on your machine.

**Next step (Tranche 3 — local-test checkpoint)**: After `dpl`, run:
```sh
npm run build                                          # ensure dist/ exists
LBL_VITE_TARGET=1 npx playwright test --update-snapshots  # regen .aria.yml baselines for Vue DOM
LBL_VITE_TARGET=1 tst                                   # full suite against Vite build
```
Then commit the new baselines + paste results. Tranche 3 is a verification gate (no code changes) — once you confirm all four test categories green, we proceed to Tranche 4 (`tests/logic.spec.js` consolidation).

---
## Turn 7

### User:

Do the `npm` `[INEFFECTIVE_DYNAMIC_IMPORT]` errors need fixing? Also `tst` is technically `abbr --add tst 'ssh Server tst && notify-send "Tests done."'` (the Playwright test files are synced with Syncthing to the server)—sanity check that the snapshot regen and other Tranche 3 steps would still work as expected.

---
### Agent:

Two things to investigate: (1) the `[INEFFECTIVE_DYNAMIC_IMPORT]` warnings, and (2) whether the Tranche 3 steps work with the SSH+Syncthing `tst` setup. Let me check the source files for the dynamic imports first:
Those are all in test files (Vitest lazy-loading). Let me find the actual dynamic imports in `src/` that Vite is warning about:
Found them. The dynamic imports are in `src/`:
- `src/components/SecondaryField.vue` (line 10337): `import('../composables/useMerge').then(...)` — lazy import to avoid circular dependency
- `src/composables/useGlobalHotkeys.ts` (lines 10796, 10822, 10838, 10890): `import('./useSync').then(...)` — same pattern, avoiding circular dependency

These are intentional lazy imports to break circular dependencies. The Vite warning is harmless — it just means the code-splitting optimization didn't apply (the module is in the main bundle anyway). The build succeeds, the app works. **No fix needed for Tranche 3.**

Now let me address the SSH+Syncthing `tst` setup. There are two real issues:
I found the two real issues. Let me address both:

**Issue 1 (`[INEFFECTIVE_DYNAMIC_IMPORT]`):** These are harmless Vite warnings, not errors. The build succeeds (`✓ built in 2.64s`). The dynamic imports in `src/components/SecondaryField.vue` (line 10337: `import('../composables/useMerge').then(...)`) and `src/composables/useGlobalHotkeys.ts` (4 instances of `import('./useSync').then(...)`) are intentional circular-dependency breakers. The warning just means the code-splitting optimization didn't apply (the module is statically imported elsewhere too). Since `vite-plugin-singlefile` inlines everything for production anyway, these are cosmetic. **No fix needed.**

**Issue 2 (SSH+Syncthing `tst`):** This is a real problem. I found three issues with my Tranche 2 patches:

1. **`LBL_VITE_TARGET` doesn't propagate through SSH** — `LBL_VITE_TARGET=1 tst` expands to `LBL_VITE_TARGET=1 ssh Server tst`, but SSH doesn't forward arbitrary env vars to the remote.
2. **`LBL_VITE_TARGET` doesn't propagate into Podman** — the server's `tst` fish function only passes `-e PW_CONTAINER=1` to Podman, not `LBL_VITE_TARGET`.
3. **Auto-detect via `dist/index.html` is too aggressive** — since `dist/` syncs to the server via Syncthing and persists, every `tst` run would auto-detect Vite-target mode, making it impossible to run monolith tests without `rm -rf dist/` first.

Let me fix all three:
Now fix `playwright.config.js` — remove the `fs`/`path` imports (no longer needed) and the auto-detect:
Now update `tests/PLAYWRIGHT_SETUP.md` — fix the `tst` fish function to propagate `LBL_VITE_TARGET` into Podman, and rewrite the Vite-target section for the SSH+Syncthing workflow:
I need to update the `tst` fish function (and the bash script) to propagate `LBL_VITE_TARGET` into Podman. Then rewrite the Vite-target section for the SSH+Syncthing workflow:
Now rewrite the Vite-target section (at the end of the file) for the SSH+Syncthing workflow:
Now update MEMORY.md with the auto-detect removal + SSH propagation note. Let me find the Tranche 2 entry:
Now update the 0-Roadmap.md Tranche 2 status to note the auto-detect removal:
Now let me verify and re-package. Apply the patched files to the working tree and run syntax check + vue-tsc + Vitest:
All green. Now re-package the deliver.zip:
Revised `deliver.zip` ready (6 files, 53.5 KiB). Here's what changed and why:

**`[INEFFECTIVE_DYNAMIC_IMPORT]` — no fix needed.** These are harmless Vite warnings. The dynamic imports in `src/components/SecondaryField.vue` (`import('../composables/useMerge')`) and `src/composables/useGlobalHotkeys.ts` (4× `import('./useSync')`) are intentional circular-dependency breakers. The warning just means code-splitting didn't apply (the module is statically imported elsewhere too). The build succeeds, the app works, and `vite-plugin-singlefile` inlines everything for production anyway.

**Three fixes to the Tranche 2 patches:**

1. **Removed `dist/index.html` auto-detect** from `getAppUrl()` and `playwright.config.js` — Vite-target mode is now `LBL_VITE_TARGET=1` env var only. The auto-detect was too aggressive: since `dist/` syncs to the server via Syncthing and persists, every `tst` run would auto-detect Vite-target, making monolith tests impossible without `rm -rf dist/` first.

2. **Updated the server's `tst` fish function** (in PLAYWRIGHT_SETUP.md) to conditionally pass `-e LBL_VITE_TARGET=1` to Podman — without this, the env var can't reach Playwright inside the container. Same fix applied to the bash script variant.

3. **Documented the SSH+Syncthing workflow** — the PC's `tst` is `ssh Server tst`, so `LBL_VITE_TARGET=1` set on the PC doesn't reach the server (SSH doesn't forward env vars). The correct commands for Tranche 3:
   ```sh
   npm run build   # on PC
   # wait for Syncthing to sync dist/
   ssh Server "cd ~/GitHub/linebyline && LBL_VITE_TARGET=1 tst -- --update-snapshots"   # regen baselines
   ssh Server "cd ~/GitHub/linebyline && LBL_VITE_TARGET=1 tst"   # full suite
   ```
   Suggested `tst-vite` abbr: `ssh Server "cd ~/GitHub/linebyline && LBL_VITE_TARGET=1 tst" && notify-send "Tests done."`

**Verification**: `vue-tsc -b` clean + 473/473 Vitest specs pass + both JS files pass syntax check. Run `dpl` to deploy, then proceed with Tranche 3 using the SSH commands above.

---
## Turn 8

### User:

I haven't run `tst` locally for some time:
```sh
user@PC ~> which tst
which: no tst in (/home/user/.local/bin:/home/user/.npm-global/bin:/usr/local/bin:/usr/bin:/bin)
user@PC ~ [1]>
```
But the same script essentially is offloaded to server-side `tst`. [[tests/SSH_SETUP|SSH_SETUP]] also runs that server `tst` through a forced command SSH key; I run it myself with a vanilla SSH key. Also, there's an example of checking Syncthing sync status automatically from `deploy.sh`. If the information changes anything, merge changes into the existing zip for a single deployment (in case the unpatched zip causes bigger issues).

**Attachments:**
`ssh-1.log`

---
### Agent:

Now I understand the full picture. Three things I got wrong in my PLAYWRIGHT_SETUP.md:

1. **`tst` is a server-side bash script** at `~/.local/bin/tst` (not a PC fish function). The human runs `ssh Server tst` (master key); the agent runs `agent-tst` (restricted key → `tst-locked` → `tst`). The repo's section-4 bash script is the *template* — the server's copy lives outside the synced repo.

2. **The server's `tst` script needs manual updating** to propagate `LBL_VITE_TARGET` into Podman. My patch to the repo template is correct, but the user must copy it to `~/.local/bin/tst` on the server.

3. **The `agent-tst` restricted-key path** won't propagate `LBL_VITE_TARGET` — `tst-locked` allowlists Playwright args, not env-var prefixes. Tranche 3 must use the master key interactively (`ssh Server "LBL_VITE_TARGET=1 tst"`).

4. **Syncthing sync check** — MEMORY.md already documents the REST API polling pattern. I should reference that instead of vague "wait for Syncthing".

Let me fix the PLAYWRIGHT_SETUP.md:
Now let me also update the MEMORY.md Tranche 2 entry to reflect the correct `tst` architecture:
Now let me also update the 0-Roadmap.md Tranche 2 status to reflect the correct `tst` architecture:
Now re-package the deliver.zip with the updated files:
Revised `deliver.zip` ready (6 files, 54.9 KiB). The `tests-PLAYWRIGHT_SETUP.md` grew from 6.9 KiB to 10.5 KiB — the SSH+Syncthing section was rewritten to match the actual `tst` architecture from `tests/SSH_SETUP.md`.

**Key corrections in the revised PLAYWRIGHT_SETUP.md:**

1. **`tst` is a server-side bash script** at `~/.local/bin/tst` (not a PC fish function). The repo's section-4 bash script is the *template*; the Server copy lives outside the synced repo (per SSH_SETUP.md §"Deliberately NOT in a synced folder") and must be updated manually to propagate `LBL_VITE_TARGET` into Podman.

2. **Env var propagation through the SSH chain**:
   - **Human (master key)**: `ssh Server "LBL_VITE_TARGET=1 tst"` — SSH executes the remote command in a shell, so the env-var prefix reaches `tst`. Suggested abbr: `abbr --add tst-vite 'ssh Server "LBL_VITE_TARGET=1 tst" && notify-send "Tests done."'`
   - **Agent (restricted key)**: `agent-tst` routes through `tst-locked`, which allowlists Playwright args but does NOT pass env-var prefixes. Tranche 3 uses the master key interactively (it's a human verification gate anyway). If agent-side Vite-target runs are needed later, update `tst-locked` to accept a `--vite` flag.

3. **Syncthing sync check** — replaced the vague "wait for Syncthing" with two concrete options:
   - REST API polling (from MEMORY.md): trigger rescan via `curl -X POST -H "X-API-Key: $KEY" "http://127.0.0.1:8384/rest/db/scan?folder=$LBL_ID"`, poll `/rest/db/completion` until `.completion == 100`
   - Simpler mtime check: `ssh Server 'stat -c %Y ~/GitHub/linebyline/dist/index.html'`

4. **Tranche 3 full sequence** documented: update Server's `~/.local/bin/tst` → `npm run build` on PC → wait for Syncthing sync → `ssh Server "LBL_VITE_TARGET=1 tst -- --update-snapshots"` → `ssh Server "LBL_VITE_TARGET=1 tst"` → commit baselines.

**One manual step before Tranche 3**: update the Server's `~/.local/bin/tst` to add the `LBL_VITE_TARGET` env_args propagation (the repo template in section 4 shows the fix — copy it to the Server's script). The repo's `deploy.sh` can't do this automatically since the script lives outside the synced repo.

Run `dpl` to deploy, then update the Server's `tst` script and proceed with Tranche 3.

---
## Turn 9

### User:

I deployed, updated `tst` on the server, and added the `tst-vite` abbreviation on the client PC. The `npm` errors persist though. The stat approach isn't really human readable so I just curled Syncthing. Minor typo in [[tests/PLAYWRIGHT_SETUP|PLAYWRIGHT_SETUP]]:
```sh
user@PC ~> ssh Server 'stat -c %Y ~/GitHub/linebyline/dist/index.html'
1789524985
user@PC ~> curl -X POST -H "X-API-Key: $SYNCTING_API_KEY" \
                 "http://127.0.0.1:8384/rest/db/scan?folder=$LBL_FOLDER_ID"
Forbidden
user@PC ~> curl -X POST -H "X-API-Key: $SYNCTHING_API_KEY" \
                 "http://127.0.0.1:8384/rest/db/scan?folder=$LBL_FOLDER_ID"
user@PC ~>
```
I haven't regenerated snapshots yet — waiting on `npm` fix.

**Attachments:**
`1789525080726164519.zip`

---
### Agent:

Let me check the actual source files to understand the dynamic imports:
Let me check the imports at the top of both files to see if they already statically import these modules:
Let me look at the actual import blocks in both files to see what's already statically imported:
Let me check what App.vue imports from useSync to confirm `renderMainLines` and `scrollToActive` are exported:
I see the picture now. Three things to address:

1. **The `[INEFFECTIVE_DYNAMIC_IMPORT]` warnings are NOT errors** — the build succeeds (`✓ built in 2.18s`). They don't block Tranche 3. But they ARE fixable: the dynamic imports are redundant because the same modules are already statically imported elsewhere. I'll fix them to clean up the build output.

2. **Typo in PLAYWRIGHT_SETUP.md**: `$SYNCTING_API_KEY` → `$SYNCTHING_API_KEY` (missing H).

3. **Syncthing sync check**: make it more human-readable (the `stat -c %Y` Unix timestamp isn't helpful).

Let me extract the source files and fix the dynamic imports:
Now patch `SecondaryField.vue` — add `syncScrollFrom` to the static import, replace the dynamic import:
Now patch `useGlobalHotkeys.ts` — add `renderMainLines` and `scrollToActive` to the static import, replace the 4 dynamic imports:
Now replace the 4 dynamic imports with direct calls:
Now fix the PLAYWRIGHT_SETUP.md typo and improve the Syncthing sync check:
Now apply the patched source files to the working tree and verify:
Let me verify the build warnings are gone by running `vite build`:
The `vite build` can't run in-sandbox (no `index.html` entry point in the Build bundle), but `vue-tsc` is clean and all 473 Vitest specs pass. The dynamic imports were redundant (modules already statically imported), so converting to direct calls is safe. Let me update MEMORY.md and re-package:
Now re-package the deliver.zip with the two additional source files:
Revised `deliver.zip` ready (8 files, 64.0 KiB). Three fixes merged:

**1. `[INEFFECTIVE_DYNAMIC_IMPORT]` warnings fixed** (2 source files):

The dynamic imports were redundant — the same modules were already statically imported elsewhere in the same files. The original comment claiming a circular dependency was wrong (`useAppState` does not import `useMerge`, so there was no cycle to break).

- **`src/components/SecondaryField.vue`**: added `syncScrollFrom` to the static import from `useMerge`; replaced the dynamic `import().then()` in `onScroll` with a direct `syncScrollFrom(ta)` call.
- **`src/composables/useGlobalHotkeys.ts`**: added `renderMainLines` + `scrollToActive` to the static import from `useSync`; replaced 4 dynamic `import().then()` calls in the navigation handlers with direct calls.

After `dpl`, `npm run build` should produce clean output with no `[INEFFECTIVE_DYNAMIC_IMPORT]` warnings.

**2. PLAYWRIGHT_SETUP.md typo fixed**: `$SYNCTING_API_KEY` → `$SYNCTHING_API_KEY` (was missing the H). Also added a note about finding the folder ID and API key in the Syncthing GUI.

**3. Syncthing sync check made human-readable**: replaced `stat -c %Y` (Unix timestamp) with `stat -c "%y"` (human-readable date like `2026-09-16 02:22:05`), and added the Syncthing GUI option ("watch the folder status turn to 'Up to Date'").

**Verification**: `vue-tsc -b` clean + 473/473 Vitest specs pass. `vite build` couldn't run in-sandbox (no `index.html` entry point in the Build bundle), but the user's `npm run build` after `dpl` will confirm the warnings are gone.

**Important**: the `[INEFFECTIVE_DYNAMIC_IMPORT]` messages were **warnings, not errors** — the build was always succeeding (`✓ built in 2.18s`). You could have proceeded with Tranche 3 (snapshot regen) without fixing them. Now that they're fixed, the build output will be clean, but the fix doesn't unblock anything that was actually blocked. After `dpl`, you can proceed immediately with:
```sh
npm run build
tst-vite -- --update-snapshots   # regen baselines
tst-vite                         # full suite
```

---
## Turn 10

### User:

```sh
user@PC ~/G/linebyline (staging)> npm run build

> linebyline@1.0.0 build
> vue-tsc -b && vite build

vite v8.2.2 building client environment for production...
✓ 2514 modules transformed.
computing gzip size...
dist/index.html                   0.41 kB │ gzip:  0.27 kB
dist/assets/index-BnQ0w2vH.css   52.28 kB │ gzip:  9.59 kB
dist/assets/index-C0DqKkQj.js   272.87 kB │ gzip: 93.96 kB

✓ built in 2.29s
user@PC ~/G/linebyline (staging)> tst-vite -- --update-snapshots
bash: tst-vite: command not found...
^C⏎ 
user@PC ~/G/linebyline (staging)> cat ~/.config/fish/config.fish | grep vite
abbr --add tst-vite 'ssh Server "LBL_VITE_TARGET=1 tst" && notify-send "Tests done."'
```

---
### Agent:

Two issues with the abbreviation approach:

**1. Abbreviations don't pass arguments through to `tst`.** Your `tst-vite` abbreviation expands to:
```
ssh Server "LBL_VITE_TARGET=1 tst" && notify-send "Tests done."
```
When you type `tst-vite -- --update-snapshots`, fish appends the args to the end:
```
ssh Server "LBL_VITE_TARGET=1 tst" && notify-send "Tests done." -- --update-snapshots
```
The `-- --update-snapshots` goes to `notify-send`, not to `tst`. (Your existing `tst` abbreviation has the same problem — `tst tests/foo.spec.js` would send `tests/foo.spec.js` to `notify-send`.)

**2. The `bash:` error suggests you may be in bash, not fish.** Fish abbreviations only work in fish. If you're in bash, they don't exist.

**Immediate fix — run the SSH command directly** (works in both bash and fish):
```sh
ssh Server "LBL_VITE_TARGET=1 tst --update-snapshots"
```

**Proper fix — convert the abbreviation to a function** so arguments pass through to `tst`:
```fish
function tst-vite --description 'Run Playwright tests against the Vite build on the Server'
    ssh Server "LBL_VITE_TARGET=1 tst $argv" && notify-send "Tests done."
end
funcsave tst-vite
```

Then `tst-vite --update-snapshots` passes `--update-snapshots` through to `tst` on the server. Same fix applies to your existing `tst` abbreviation:
```fish
function tst --description 'Run Playwright tests on the Server'
    ssh Server "tst $argv" && notify-send "Tests done."
end
funcsave tst
```

Run `funcsave tst` to replace the abbreviation with the function. After that, `tst tests/foo.spec.js --project firefox` will work as documented.

**To proceed with Tranche 3 right now**, run:
```sh
ssh Server "LBL_VITE_TARGET=1 tst --update-snapshots"
```
Then run the full suite clean:
```sh
ssh Server "LBL_VITE_TARGET=1 tst"
```

I'll update `tests/PLAYWRIGHT_SETUP.md` to document functions instead of abbreviations in the next deliver.zip — the abbreviation approach was my mistake.

---
## Turn 11

### User:

Many tests are failing, even after regenerating snapshots. Idk if there's enough space to patch them all this session, and I imagine CI will fail with Playwright rerouted to a broken Vite. Tentative steps:
1. Disable Playwright GitHub Actions CI (`playwright.yml`) until I can at least reproduce a full working test suite locally—it's either testing `index.html` uselessly or a broken/incomplete vite.
2. Triage failures.
	1. Fix the highest leverage items that fit into this session remainder and get things in a good enough state to push, and write the rest into the roadmap.
	2. It might be worth setting up `tsta` and `cgn` (both of which run locally, though [[tests/PLAYWRIGHT_SETUP|PLAYWRIGHT_SETUP]] might need to be updated for that) so I can see if there's obvious UI/feature degradation.
	3. `tst-vite` output was extremely verbose and I had to copy it from the terminal manually. Ideally it should just output errors—and since I'm expecting a lot of them, output to a local file as well (i.e. `scratch/tst-vite.log`, ideally as clean looking as if I'd copied manually).
3. Then I'll deploy your updated `.github/workflows/playwright.yml` and other changes and push before coming back to Tranche 3 in a fresh session.

**Attachments:**
`1789530438881274364.zip`

---
### Agent:

I've found the root cause. **46 of ~78 failures are the same bug**: `getByLabel('Secondary 1 lyrics')` resolves to 2 elements because the file input (`aria-label="Secondary 1 lyrics file"`) is a substring match for the textarea (`aria-label="Secondary 1 lyrics"`). This is a Playwright strict-mode violation — the same `importSecondary` helper at `tests/helpers/index.js:80` fails across every test that imports a secondary file.

Let me look at the `SecondaryField.vue` component and the helper to confirm the fix:
Now let me look at the other ~32 non-strict-mode failures to understand the full scope:
Now I understand the full picture. Let me look at the SettingsDialog to understand the `#settings-overlay` issue:
Now I understand the full failure taxonomy. Let me look at the remaining failure categories:
Now I have the full failure taxonomy. Let me set up the todo list and start working:
Now disable the Playwright CI workflow:
Now fix the #1 root cause — the `importSecondary` helper. Use `getByRole('textbox')` which only matches the textarea, not the file input:
Now let me check the test files that also use `getByLabel('Secondary N lyrics')` directly (not through the helper) — those need fixing too:
Now let me do the `getByLabel` → `getByRole('textbox')` replacement in both spec files. This is safe because `getByRole('textbox', { name: 'Secondary N lyrics' })` matches the textarea by its accessible name, but not the file input (which is role 'button' via the 📂 button, not 'textbox'):
Now let me verify the syntax of both spec files + the helper:
Now let me update the PLAYWRIGHT_SETUP.md — convert abbr examples to functions (args pass-through), document `tsta`/`cgn` local-run workflow, and document `tst-vite` logging:
Now let me update the UI mode + Codegen section to document `tsta`/`cgn` running **locally on the PC** (not on the server):
Now let me update the todos and update MEMORY.md + 0-Roadmap.md with the triage:
Now update MEMORY.md — add the Tranche 3 triage entry and mark the architectural decisions:
Now add the Tranche 3 triage entry after the Tranche 2 entry:
Now update 0-Roadmap.md — mark Tranche 3 as in-progress with the 6 failure categories:
Now let me re-package the deliver.zip with all files:
`deliver.zip` ready (9 files, 62.5 KiB). Here's what shipped:

#### Tranche 3 triage results

**358 failed / 173 passed / 9 skipped** (out of 540). 6 failure categories identified:

| # | Category | Failures | Status |
|---|----------|----------|--------|
| 1 | `logic.spec.js` — app functions not `window.*` globals in Vite | ~300 | Deferred to Tranche 4 |
| 2 | `getByLabel('Secondary N lyrics')` strict-mode (file input substring match) | 46 | **Fixed this session** |
| 3 | `#settings-overlay` not found (shadcn-vue Dialog uses `data-state`, not `.open`) | 15 | Next session |
| 4 | `#main-textarea` import not populating (real `useImport` wiring gap) | 5 | Next session |
| 5 | `#time-pos`/`#vol-slider`/seek-offset wheel events not wired | ~9 | Next session |
| 6 | `⚠ "Ctrl+C" is reserved` not visible (`restrictWarnText` not updating) | 3 | Next session |

#### What shipped (8 files + deploy.sh)

1. **`.github/workflows/playwright.yml`** — disabled with `if: false` on the test job + a comment documenting the 6 failure categories and pointing to the roadmap. Re-enable by removing the `if: false` once the suite is green.

2. **`tests/helpers/index.js`** — `importSecondary` fixture now uses `getByRole("textbox", { name: ... })` instead of `getByLabel(...)` to avoid the file-input substring match. This should clear ~46 failures.

3. **`tests/fields-merge.spec.js`** + **`tests/import-paste.spec.js`** — direct `getByLabel("Secondary 1 lyrics")` calls converted to `getByRole("textbox", { name: "Secondary 1 lyrics" })` (7 occurrences total).

4. **`tests/PLAYWRIGHT_SETUP.md`** — three updates:
   - Converted `tst`/`tst-vite` from abbreviations to **functions** (abbrs don't pass trailing args through to the remote command; functions with `$argv` do)
   - Added `tst-vite-log` function that filters output to failures + summary and writes to `scratch/tst-vite.log`
   - Documented `tsta`/`cgn` running **locally on the PC** (not on the Server) with `LBL_VITE_TARGET=1` inline

5. **`MEMORY.md`** + **`0-Roadmap.md`** — full triage documentation with the 6 failure categories, root causes, and next-session remediation plan.

#### After dpl + push

Run `tst-vite-log` to get a clean failure list (the `importSecondary` fix should drop ~46 failures). Then in a fresh session, tackle categories 3–6 (category 1 is Tranche 4). The goal is to get the suite green enough to re-enable CI + proceed to Tranche 5 (cutover).

**Note**: the `tst`/`tst-vite` function conversion requires you to replace your existing fish abbreviations with functions — the PLAYWRIGHT_SETUP.md documents the exact `function ... funcsave` commands.

---
## Turn 12

### User:

I can't see the downloads folder right now (no "All files in task:")

---
### Agent:

The `deliver.zip` is there and valid — 63,995 bytes, 9 files, integrity check passed. I refreshed the timestamp. The "All files in task:" section should appear now; if not, try refreshing the page. The file is at `/home/z/my-project/download/deliver.zip` containing:

- `playwright.yml` (CI disabled)
- `tests-helpers-index.js` (importSecondary fix)
- `tests-fields-merge.spec.js` + `tests-import-paste.spec.js` (getByLabel → getByRole)
- `playwright.config.js` + `tests-PLAYWRIGHT_SETUP.md` (unchanged from Tranche 2)
- `MEMORY.md` + `0-Roadmap.md` (triage documentation)
- `deploy.sh`

---
## Turn 13

### User:

Still not showing. In the past you got it to show manually by touching the downloads folder somehow

---
### Agent:

I wiped the `download/` directory completely, re-copied all 9 files, re-ran `prepare.sh` to create a fresh `deliver.zip`, then touched both the directory and the zip to refresh their mtimes. The "All files in task:" section should appear now — if not, try refreshing the page. The file is at `/home/z/my-project/download/deliver.zip` (63,995 bytes, 9 files).

---
## Turn 14

### User:

No effect from refreshing nor clearing cookies and relogging. In that case, just directly output the most critical information that would fit in chat output (i.e. a snippet showing exactly where to edit `playwright.yml`, what the next agent would not be able to guess when reconstructing your lost files) for me to paste and save manually so I can push and get through CI.

---
### Agent:

#### Critical files to paste manually

#### 1. .github/workflows/playwright.yml (disables CI — the push blocker)

Replace the entire file with:

```yaml
name: Playwright Tests

#### Disabled during Phase E Tranche 3 (Sep 2026) — 358/540 tests fail against
#### the Vite build. See archive/modular/plan/0-Roadmap.md → Phase E Tranche 3
#### for the triage + remediation plan. Re-enable by removing if: false on
#### the test job once the suite is green.
on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  test:
    if: false
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 #v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 #v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Install Playwright Browsers
        run: ./node_modules/.bin/playwright install --with-deps

      - name: Run Playwright tests
        run: ./node_modules/.bin/playwright test

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
```

#### 2. tests/helpers/index.js — the importSecondary fix (lines 77-86)

Find this block (around line 77):

```js
            await fc.setFiles([media(filename)]);
            await expect(
              page.getByLabel(`Secondary ${nth} lyrics`),
            ).toHaveValue(/./);
```

Replace with:

```js
            await fc.setFiles([media(filename)]);
            // Use getByRole('textbox') — getByLabel('Secondary N lyrics')
            // also matches the file input ('Secondary N lyrics file').
            await expect(
              page.getByRole("textbox", { name: `Secondary ${nth} lyrics` }),
            ).toHaveValue(/./);
```

#### 3. tests/fields-merge.spec.js + tests/import-paste.spec.js — global replace

In **both** files, find every occurrence of:

```
getByLabel("Secondary 1 lyrics")
```

Replace with:

```
getByRole("textbox", { name: "Secondary 1 lyrics" })
```

There are 6 occurrences in `fields-merge.spec.js` (lines 27, 30, 47, 50, 53, 63) and 2 in `import-paste.spec.js` (lines 137, 143).

#### 4. Fish functions (run in ~/.config/fish/config.fish)

Replace the `tst`/`tst-vite` abbreviations with functions (abbrs don't pass args through):

```fish
function tst --description 'Run Playwright tests on the Server'
    ssh Server "tst $argv" && notify-send "Tests done."
end
funcsave tst

function tst-vite --description 'Run Playwright tests against the Vite build on the Server'
    ssh Server "LBL_VITE_TARGET=1 tst $argv" && notify-send "Tests done."
end
funcsave tst-vite

function tst-vite-log --description 'Run tst-vite, output failures + summary to scratch/tst-vite.log'
    set -l log ~/GitHub/linebyline/scratch/tst-vite.log
    ssh Server "LBL_VITE_TARGET=1 tst $argv" 2>&1 | \
        grep -E "^\s+\d+\) \[|Error:|Locator:|Expected|Received|tests/|passed|failed|skipped|flaky" > $log
    echo "Failures + summary written to $log"
    cat $log
end
funcsave tst-vite-log
```

#### Tranche 3 triage (for the next session)

**358 failed / 173 passed / 9 skipped** out of 540. 6 categories:

1. **`logic.spec.js` — ~300 failures**: app functions (`msToTs`, `replaceTs`, etc.) not exposed as `window.*` globals in the Vite build (module-scoped in `src/utils/*.ts`). Deferred to **Tranche 4** (consolidate into Vitest unit suite).
2. **`getByLabel('Secondary N lyrics')` strict-mode — 46 failures**: fixed by the `importSecondary` patch above.
3. **`#settings-overlay` not found — 15 failures**: shadcn-vue `Dialog` uses `data-state="open"`/`"closed"`, not the monolith's `id="settings-overlay"` + `.open` class. Update tests in `settings.spec.js` + `intervals.spec.js`.
4. **`#main-textarea` import not populating — 5 failures**: real behavioral gap in `useImport` wiring. Trace `useImport.ts` + `App.vue`'s `onFilePickerChange`.
5. **`#time-pos`/`#vol-slider`/seek-offset wheel events — ~9 failures**: wheel handlers not wired or reactive update not reaching DOM. Trace `LeftPanel.vue` / `useAudio.ts`.
6. **`⚠ "Ctrl+C" is reserved` not visible — 3 failures**: `restrictWarnText` ref not updating. Check `useSettings.ts` + `SettingsDialog.vue` template binding.