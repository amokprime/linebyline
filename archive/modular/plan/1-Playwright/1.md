---
model: GLM-5.1
---
Follow `*-SKILL.md` files as rules. This is the Onboard step.

---
Now let me read the key SKILL.md files in full:
Now let me check for the Boilerplate.md and any existing project files:
I've read the full `repomix-onboard.xml` and all the SKILL.md files it contains. Now I'll follow the **Onboard step** as defined in `project-workflow-SKILL.md`:

**Step 1 — Onboard checklist:**

1. ✅ Read `project-workflow-SKILL.md` — done
2. ✅ Read `README.md` — done  
3. ⬜ Read the web-channel skill — not included in the onboard bundle; I'll apply its rules when available
4. ⬜ Note the current app version — you haven't stated it, and I don't have the project tree to grep for `<title>`
5. ⬜ Create the companion .md file in downloads
6. ✅ Do not begin coding

I need the **current app version** to name the companion file and set the baseline. From the archive directory structure I can see versions up through `0.37.2`, and the Roadmap references `0.38.0` as future. 

**What is the current app version?** (e.g. "0.37.2" or a filename like "linebyline-0.37.2.html")

---

**Here's what I've onboarded so far:**

| Knowledge | Source |
|---|---|
| Project: LineByLine — single-file HTML app for manual line-by-line lyrics syncing | README.md |
| Architecture: 2700-line monolith (`docs/index.html`), CommonJS, Playwright test suite | package.json, Roadmap |
| Roadmap: 6-step refactor plan (Playwright CI → ESLint → Vite+Vue modular → TypeScript → Security → UI) | 0-Roadmap.md |
| ESLint in Zed: Already set up, resolved 6782→7 issues by excluding `.obsidian/` + `local/`, fixing spec globals | 2-Zed-ESLint.md |
| Workflow: Onboard → Build → Review → Test → Skills → Propose → Wrap Up, Repomix-based just-in-time context | project-workflow-SKILL.md |
| Skills system: `topic-SKILL.md` in `ai/z-ai-glm/skills/`, frontmatter-driven triggering, lean & generalized writing | skill-SKILL.md |
| Companion files: version-named `.md` chat transcripts in `download/`, not readmes | project-workflow-SKILL.md |
| Memory.md discipline: one bullet per parallel thread, updated in-place, not per-turn changelog | project-workflow-SKILL.md |

---
I uploaded the web channel skill in `Archive.zip`. The current app version is 0.37.2, but we won't be working on app code this session. I uploaded the Test Repomix, but this session will actually focus on Playwright CI setup. Qwen Coder agent (coder.qwen.ai) generated a `playwright.yml` for [[archive/modular/plan/0-Roadmap#1. Playwright CI + Local Workflow|0-Roadmap]] and an edit of `tests/helpers/index.js`. See the `chat.md`. Sanity check its work in context of how my tests are setup now and my ambition to move away from the `archive/semantic` clutter of `.html` files (working on `docs/index.html` instead).

---
I've thoroughly searched the uploaded files but the `Archive.zip` appears to contain another project archive (ChatGPT conversations, repomix, Python modules), not the LineByLine-specific files I need. I cannot find:

1. **The web-channel skill** (`web-channel-SKILL.md`) — not in the archive or onboard bundle
2. **The Test Repomix** (`repomix-test.xml`) — the `repomix-output.xml` in Archive.zip is for another project, not LineByLine
3. **The `playwright.yml`** generated by Qwen Coder
4. **The `tests/helpers/index.js` edit** by Qwen Coder
5. **The `chat.md`** companion transcript

To do the sanity check, I need the actual Qwen Coder output. Could you upload:

- **`chat.md`** (the Qwen Coder session transcript) — this should contain the `playwright.yml` content and the `index.js` diff
- **`repomix-test.xml`** (the Test Repomix) — so I can see `playwright-testing-SKILL.md`, the current `playwright.config.js`, and the existing `tests/helpers/index.js`
- **The `playwright.yml`** and **`index.js`** files directly if you have them separate from the chat.md

In the meantime, let me establish what I **can** sanity-check from what I have — the Roadmap requirements for Playwright CI and the known issues with `findLatestVersion()`:
Here's what I **already know** from the onboard context that will frame the sanity check once I have the files:

### Roadmap Item 1 Requirements (the spec Qwen Coder should have followed)

From `0-Roadmap.md`:

| Requirement | Detail |
|---|---|
| **CI trigger** | Push + PRs |
| **Runner** | Ubuntu with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` (Node 20 removed from runners Sept 2026) |
| **Steps** | `npm ci` → `npx playwright install --with-deps` → `npx playwright test` (Chromium + Firefox + WebKit) |
| **Artifact upload** | Test results on failure |
| **Serve strategy** | Tests currently scan `archive/semantic/` via `findLatestVersion()` — this **breaks** during the modular refactor and must be updated to point at the Vite build output |
| **Firefox caveat** | Flakiest of the three browsers; protocol error on context teardown after downloads |

### Critical known bug in `tests/helpers/index.js`

From the ESLint session (`2-Zed-ESLint.md`):

```js
// BUG (line 35): b.patch - b.patch is always 0
.sort((a, b) => b.major - a.major || b.minor - a.minor || b.patch - b.patch)
// FIX:
.sort((a, b) => b.major - a.major || b.minor - a.minor || b.patch - a.patch)
```

This means `findLatestVersion()` has been **silently picking the wrong "latest"** any time two versions share a major.minor (e.g. `0.35.9` vs `0.35.10`).

### The `archive/semantic` → `docs/index.html` transition

Your stated ambition: move away from the clutter of versioned HTML files in `archive/semantic/` and work on `docs/index.html` directly. This means the CI workflow needs a **serve strategy** that:
1. Serves `docs/index.html` (not scans `archive/semantic/`)
2. Is compatible with the future Vite refactor (where `npm run build` → `dist/` becomes the serve target)

---

**Please upload the Qwen Coder's `chat.md` (and the `playwright.yml` + `index.js` if you have them separately), plus the Test Repomix, so I can do the actual sanity check.**

---
That's very strange. I renamed the zip and reuploaded in case you have a cached `Archive.zip` from another session (which shouldn't be possible anyway as I released all three of my sandboxes from my chat.z.ai account settings).

---
Now I have everything. Let me read all five files:
Now let me read the Test Repomix to understand the current test setup:
Now let me also read the current `tests/helpers/index.js` from the repomix and the `PLAYWRIGHT_SETUP.md`:
Now let me also look at the uploaded image to see if it contains relevant info:
Now I have all the context. Let me do the thorough sanity check. Here's my analysis:

---

## Sanity Check: Qwen Coder's Playwright CI Output

### 1. `playwright.yml` — 5 issues, 2 critical
Let me compare the Qwen Coder output against what the Roadmap and the existing `playwright.config.js` require:

**Qwen Coder's `playwright.yml`:**
```yaml
name: Playwright Tests
on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npx playwright test
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
```

**Existing `playwright.config.js`** (from repomix-test.xml):
```js
const inCI = !!process.env.CI;
const enableWebkit = !!process.env.PW_CONTAINER || inCI;
export default defineConfig({
  forbidOnly: inCI,
  retries: inCI ? 2 : 0,
  workers: inCI ? 1 : undefined,
  use: { baseURL: "http://localhost:3004", trace: "on-first-retry" },
  outputDir: "./trash",
  projects: [
    { name: "chromium", ... },
    { name: "firefox", ... },
    ...(enableWebkit ? [{ name: "webkit", ... }] : []),
  ],
  webServer: {
    command: "npx serve . -l 3004",
    url: "http://localhost:3004",
    reuseExistingServer: !inCI,
  },
});
```

Here are the findings:

#### ❌ **CRITICAL: Missing `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` env var**

The Roadmap explicitly states: *"Ubuntu runner with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` (Node 20 removed from runners September 2026; this env var opts into Node 24 now)"*. The `2-Zed-ESLint.md` confirms: *"Only implemented for existing codeql.yml and sonarcloud.yml"*. This is a known CodeQL flag — without it, GitHub Actions runs Node 20 which is deprecated and will be removed.

**Fix:** Add `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` to the job's `env` block, or set it at the workflow level.

#### ❌ **CRITICAL: `node-version: lts/*` is ambiguous and wrong for this project**

`lts/*` resolves to whatever NPM currently considers "latest LTS" — this is a moving target. Your `package.json` has `"type": "commonjs"` and your Playwright is `@playwright/test@^1.61.0`. The Roadmap says Node 24 (via the env var), and you're on Fedora 44 with Node 24 locally. Using `lts/*` could give Node 22 today and Node 24 tomorrow, with no control over when your CI breaks from a Node version change.

**Fix:** Pin to `node-version: 24` to match the Roadmap's intent and your local environment.

#### ⚠️ **MODERATE: `actions/upload-artifact` uses `if: always()` — uploads even on success**

The Roadmap says *"Upload test results artifact on failure"*. `if: always()` uploads on every run, including successes. This wastes artifact storage (30-day retention × every green run). The Playwright HTML report is only useful for debugging failures.

**Fix:** Change to `if: failure()` so artifacts are only uploaded when tests fail. You can still download the report from the Actions run summary if you really want it on success.

#### ⚠️ **MODERATE: Missing `CI=1` env var awareness**

Your `playwright.config.js` uses `const inCI = !!process.env.CI;` to enable webkit, set `retries: 2`, `workers: 1`, and `forbidOnly: true`. GitHub Actions **automatically sets `CI=1`** on all runners, so this works without explicit config — good. But it's worth documenting this dependency in the workflow file as a comment, so a future contributor doesn't accidentally remove it.

#### ℹ️ **MINOR: `staging` branch trigger**

The Roadmap says *"Triggers on push and PRs"*. Qwen Coder added `staging` as a branch filter because you told it *"I typically work from my staging branch and open a PR"*. This is reasonable and doesn't conflict with the Roadmap (which only mentions `main` as the minimum). The dual-branch trigger is a good addition for your workflow.

---

### 2. `tests/helpers/index.js` edit — 4 issues, 2 critical
**Qwen Coder's change:** Replace `findLatestVersion()` with `getAppUrl()` that hardcodes `/docs/index.html`.

**Current `index.js` exports:**
```js
module.exports = { findLatestVersion, test, expect, tabUntilFocused, waitForLyrics, waitForAudio, waitForImport };
```

**Qwen Coder's modified exports:**
```js
module.exports = { getAppUrl, test, expect, tabUntilFocused };
```

#### ❌ **CRITICAL: Dropped `waitForLyrics`, `waitForAudio`, `waitForImport` from exports**

These are used throughout the test suite:
- `waitForImport` — used in `import-paste.spec.js`, `undo-redo.spec.js`, `intervals.spec.js`, `accessibility.spec.js`, `playback.spec.js`
- `waitForLyrics` — used directly in `undo-redo.spec.js`
- `waitForAudio` — used directly in `intervals.spec.js`

Removing these from `module.exports` will **break every test that imports audio or lyrics**. This is a showstopper.

#### ❌ **CRITICAL: Dropped `workaroundPaste` fixture**

The current `index.js` defines a `workaroundPaste` fixture (lines 31266–31275) that:
1. Skips clipboard tests on webkit (unreliable)
2. Grants `clipboard-read`/`clipboard-write` permissions on chromium

Multiple tests use `workaroundPaste: _workaroundPaste`. Qwen Coder's version completely removes this fixture. Any clipboard-paste test will now fail on webkit (empty snapshots) and may fail on chromium (no clipboard permissions).

#### ⚠️ **MODERATE: Dropped `importSecondary`'s `await expect(...).toHaveValue(/./)` wait guard**

Current code:
```js
importSecondary: async ({ page, media }, use) => {
  await use(async (nth, filename) => {
    const [fc] = await Promise.all([...]);
    await fc.setFiles([media(filename)]);
    await expect(page.getByLabel(`Secondary ${nth} lyrics`)).toHaveValue(/./); // ← wait guard
  });
},
```

Qwen Coder's version omits the `await expect(...).toHaveValue(/./)` line. This wait guard ensures the secondary textarea is populated before the test continues. Without it, tests that import secondary files and then immediately interact with the textarea will race.

#### ⚠️ **MODERATE: `getAppUrl()` hardcodes path — breaks `cgn` codegen helper**

Your Fish `cgn` function in `PLAYWRIGHT_SETUP.md` uses `findLatestVersion()`:
```fish
set -l path (node -e "const{findLatestVersion}=require('@linebyline/test-helpers');process.stdout.write(findLatestVersion())")
npx playwright codegen "http://localhost:3004$path"
```

Renaming `findLatestVersion` → `getAppUrl` breaks this. The codegen helper needs updating too. This isn't a bug in the CI change per se, but it's a breaking change to your local tooling that Qwen Coder didn't flag.

#### ℹ️ **MINOR: The `findLatestVersion()` sort bug was NOT fixed**

The existing `b.patch - b.patch` bug (always 0) is present in the original file. Qwen Coder replaced the entire function instead of fixing the bug. Since `getAppUrl()` doesn't sort versions at all, the bug is moot — but it means the old `findLatestVersion()` is gone, so you can't fall back to it if you need to test against an archived version for regression investigation.

---

### 3. Overall CI workflow assessment
**The core idea is right:** replacing `findLatestVersion()` with a function that targets `docs/index.html` directly is exactly the transition you described — away from the `archive/semantic` clutter. This aligns with the Roadmap's Phase E ("Update `findLatestVersion()` → point at Vite build output"), just earlier in the timeline.

**But the execution has gaps for the `docs/index.html` serving strategy:**

Your `playwright.config.js` already handles this correctly:
```js
webServer: {
  command: "npx serve . -l 3004",
  url: "http://localhost:3004",
  reuseExistingServer: !inCI,
},
```

`npx serve .` serves the project root on port 3004. With `baseURL: "http://localhost:3004"` and `getAppUrl()` returning `/docs/index.html`, tests will navigate to `http://localhost:3004/docs/index.html` — which is exactly right. The CI workflow doesn't need any special serve step because `playwright.config.js`'s `webServer` config handles it automatically. ✅

**One transition concern:** When the Vite refactor lands (Roadmap item 3), the web server command changes from `npx serve . -l 3004` to `npx vite preview` (or the Vite dev server), and `getAppUrl()` would return `/` instead of `/docs/index.html`. That's a future change, not a current problem — but it's worth a comment in `getAppUrl()` so the next agent knows.

---

### 4. Recommended fixes

Here's what I'd correct, in priority order:

**`playwright.yml` — corrected version:**

```yaml
name: Playwright Tests

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
```

Changes from Qwen Coder's version:
1. Added `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` env var
2. Pinned `node-version: 24` instead of `lts/*`
3. Changed `if: always()` → `if: failure()` for artifact upload

**`tests/helpers/index.js` — minimal diff from current (preserving all exports and fixtures):**

```js
/**
 * Returns the URL path to the main app entry point.
 * Currently targets docs/index.html. After the Vite refactor
 * (Roadmap item 3), this will return "/" and the webServer
 * command will change to `npx vite preview`.
 *
 * @returns {string} URL path like /docs/index.html
 */
function getAppUrl() {
  return "/docs/index.html";
}
```

Then change the `page` fixture from:
```js
page: async ({ page }, use) => {
  await page.goto(findLatestVersion());
  await use(page);
},
```
to:
```js
page: async ({ page }, use) => {
  await page.goto(getAppUrl());
  await use(page);
},
```

And update exports:
```js
module.exports = {
  getAppUrl,          // replaces findLatestVersion
  test,
  expect,
  tabUntilFocused,
  waitForLyrics,
  waitForAudio,
  waitForImport,
};
```

**Key differences from Qwen Coder's version:**
- Preserves `workaroundPaste` fixture
- Preserves `waitForLyrics`, `waitForAudio`, `waitForImport` exports
- Preserves `importSecondary`'s `toHaveValue(/./)` wait guard
- Doesn't do a full-file rewrite — just swaps `findLatestVersion()` → `getAppUrl()` and updates the exports
- Adds a comment about the future Vite transition

**Fish `cgn` function update** (in `~/.config/fish/functions/` or `config.fish`):
```fish
function cgn
  cd ~/GitHub/linebyline
  set -l path (node -e "const{getAppUrl}=require('@linebyline/test-helpers');process.stdout.write(getAppUrl())")
  npx playwright codegen "http://localhost:3004$path" $argv
end
```

---

### Summary verdict

| Aspect | Qwen Coder's output | Verdict |
|---|---|---|
| **playwright.yml structure** | Correct shape (checkout → node → npm ci → install → test → upload) | ✅ Good |
| **Node version** | `lts/*` (moving target) | ❌ Fix: pin to 24 |
| **Node 24 env var** | Missing | ❌ Fix: add `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` |
| **Artifact upload condition** | `if: always()` | ⚠️ Fix: `if: failure()` |
| **Branch triggers** | main + staging | ✅ Reasonable extension |
| **index.js: getAppUrl() concept** | Replaces `findLatestVersion` with `docs/index.html` target | ✅ Right idea |
| **index.js: dropped exports** | Missing `waitForLyrics`, `waitForAudio`, `waitForImport` | ❌ **Breaks most tests** |
| **index.js: dropped workaroundPaste** | Removed entirely | ❌ **Breaks clipboard tests** |
| **index.js: dropped importSecondary guard** | Missing `toHaveValue(/./)` wait | ⚠️ Race condition |
| **index.js: local tooling** | Didn't update `cgn` Fish function | ⚠️ Breaks codegen |

**Bottom line:** The `playwright.yml` needs 3 targeted fixes. The `index.js` edit needs to be redone as a minimal diff — replacing `findLatestVersion` → `getAppUrl` while preserving all existing fixtures and exports. Qwen Coder did a full-file rewrite that dropped critical pieces.
Now let me create the companion file and corrected deliverables:
Now write the corrected files:
Sanity check complete. **Qwen Coder got the idea right but the execution wrong** — the `index.js` rewrite would break most of your test suite by dropping `waitForImport`, `waitForLyrics`, `waitForAudio`, and the `workaroundPaste` fixture.

Corrected files in `download/`:
- **`playwright-v1.yml`** — fixed: `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` env var, `node-version: 24`, `if: failure()` on artifact upload
- **`helpers-index-v1.js`** — minimal diff from current: swaps `findLatestVersion()` → `getAppUrl()` targeting `/docs/index.html`, preserves all fixtures and exports (`workaroundPaste`, `waitForImport`, etc.), keeps the `importSecondary` wait guard

**To apply:** rename `playwright-v1.yml` → `.github/workflows/playwright.yml`, replace `tests/helpers/index.js` with `helpers-index-v1.js`, and update your Fish `cgn` function to use `getAppUrl` instead of `findLatestVersion`. No snapshot regeneration needed — `docs/index.html` is the same app that was served from `archive/semantic/0.37.2/`.

---
Also see my existing workflows as examples and a SHA pinning step I had to follow recently because Sonar flagged it.

---
The LineByLine files are already extracted under `/home/z/my-project/upload/LineByLine/`. Now let me extract the new `SHA-pin.zip` and read all the key files in parallel.
Good — SHA-pin.zip contains 4 files. Let me now read everything in parallel:
Now let me read all the LineByLine files in parallel:
The repomix-test.xml is large. Let me search for the key files within it:
Let me read the actual file contents from the repomix:
Now let me also check the onboard repomix for the roadmap:
Now I have all the data. Let me compile the full sanity check. Let me also verify which fixtures the test specs actually import:
I now have all the evidence needed for a thorough sanity check. Let me write the companion report:
Sanity check complete. Here's the summary:

**Qwen Coder's output has 3 critical issues that will break things:**

1. **`playwright.yml` missing `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`** — without this, the workflow runs on Node 20 which was removed from runners in Sept 2026. Your existing `codeql.yml` already has it; this file needs the same top-level `env` block.

2. **`index.js` deleted 3 helper exports** (`waitForLyrics`, `waitForAudio`, `waitForImport`) — these are imported by 15+ spec files. Removing them causes immediate `ReferenceError` at test load.

3. **`index.js` deleted `workaroundPaste` fixture** — used by 9 paste tests across 3 spec files. Without it, webkit won't skip paste tests (empty snapshots) and chromium won't get clipboard permissions.

Plus 2 high-severity issues: no SHA pinning on action refs (SonarCloud S7637 will flag), and your `sonarcloud.yml` also lacks `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` (pre-existing).

The good news: the `archive/semantic` → `docs/index.html` migration direction is correct, the branch config `[main, staging]` is good, and the `b.patch - b.patch` sort bug is eliminated (by removal). The corrected versions of both files are in the report.

Next step: apply the fixes from §4 and §5 of the report, resolve the two remaining SHA pins (`actions/setup-node@v4`, `actions/upload-artifact@v4`), and update the downstream items in §6 (`cgn` alias, skill doc, roadmap text).

---
I updated:
- [x] The new workflow files you generated
- [x] The SHA hashes in `playwright.yml`
- [x] The top-level node24 env in `sonarcloud.yml`
- [x] `0-Roadmap.md` item 1
- [x] `playwright-testing-SKILL.md`
- [x] The `index.js` you generated as-is (`tst -g title` passed after dropping it in)

`cgn`, `tst`, and `tsta` are fish functions (`~/.config/fish/functions/`) and I haven't changed them yet.

---
The fish functions look good. Only `cgn.fish` needs updating — `tst` and `tsta` don't reference `findLatestVersion` at all (the navigation is handled inside the test helper + `playwright.config.js`'s `webServer`).

**`cgn.fish` issue:** You commented out the `findLatestVersion` path resolution (lines 3–4), but the replacement on line 5 opens `http://localhost:3004` — that's the project root served by `npx serve`, which gives a directory listing, not the app. With the migration to `docs/index.html`, codegen should target `http://localhost:3004/docs/index.html`.
Here's the breakdown:

| Function | Needs change? | Why |
|---|---|---|
| `cgn.fish` | **Yes** | Currently opens `http://localhost:3004` (directory listing). Must target `http://localhost:3004/docs/index.html` to point codegen at the app. |
| `tst.fish` | No | Navigation is handled by `getAppUrl()` inside the test helper. `PW_CONTAINER=1` + `playwright.config.js` `webServer` does the rest. |
| `tsta.fish` | No | Same — test helper + config handle navigation. |

The updated `cgn.fish` is in download. Drop it into `~/.config/fish/functions/` to replace the current one. The only real change is the URL path — everything else (the `cd`, the `$argv` pass-through) stays the same.

---
```sh
user@PC ~/G/linebyline (main)> cat ~/.config/fish/functions/cgn.fish
function cgn
    cd ~/GitHub/linebyline
    # Previously: findLatestVersion() resolved /archive/semantic/X.X.X/linebyline-X.X.X.html
    # Now: app lives at docs/index.html (see 0-Roadmap item 1 migration)
    npx playwright codegen "http://localhost:3004/docs/index.html" $argv
end
user@PC ~/G/linebyline (main)> cgn
node:internal/process/promises:394
    triggerUncaughtException(err, true /* fromPromise */);
    ^

[PlaywrightError: net::ERR_CONNECTION_REFUSED at http://localhost:3004/docs/index.html
Call log:
  - navigating to "http://localhost:3004/docs/index.html", waiting until "load"
] {
  log: [
    '  - navigating to "http://localhost:3004/docs/index.html", waiting until "load"'
  ]
}

Node.js v22.23.1
user@PC ~/G/linebyline (main) [1]>
```

---
Right — `cgn` needs the dev server running first. The old version had the same problem (it would fail identically if port 3004 wasn't up). Two options:

1. **Just start the server manually** — `npx serve . -l 3004 &` then `cgn`
2. **Make `cgn` auto-start the server** — like `tst`/`tsta` rely on `playwright.config.js`'s `webServer` to handle this, but codegen doesn't use the config's `webServer` option

The practical fix is option 2 — check if the server is already listening, and if not, start it in the background:
The updated `cgn.fish` now auto-starts `npx serve . -l 3004` if nothing's listening on that port, waits up to 5 seconds for it to be ready, then opens codegen. If the server is already running (e.g., from a previous `tsta` session), it skips the startup.

`ss -tlnq` checks for a listening TCP socket on port 3004 — works on Fedora without needing `lsof` or `netstat`. Replace it with `lsof -i :3004` if you prefer, but `ss` is preinstalled on basically every Linux distro.

---
If I run `cgn` in a terminal like Konsole, Ghostty, or Zed terminal and then close the program, will the server still be left silently running in the background? That's not ideal if so. In the past I ran another fish function, `srv`, that I could close the window for to stop:
```fish
function srv
    cd ~/GitHub/linebyline
    npx serve . -l 3004
end
```
Of course, not having to do that in a separate window would be better.

---
Good catch. Yes — with `&`, the `serve` process becomes a background job of the shell. If you close the terminal, SIGHUP kills it (fine), but if you just close the codegen window and the `codegen` command exits normally, the function continues and we can clean up. The trick is to add cleanup after `codegen` exits, and only kill the server if `cgn` started it:
Lifecycle breakdown:

| Exit scenario | Server killed? | How |
|---|---|---|
| Close codegen window | ✅ Yes | `codegen` exits normally → cleanup runs → `fuser -k` |
| Ctrl+C | ✅ Yes | Fish interrupts `codegen` but **continues executing the function** → cleanup runs |
| Close terminal window | ✅ Yes | SIGHUP kills the shell and all its children (including the backgrounded `serve`) |

The `need_cleanup` flag ensures we only `fuser -k` if `cgn` started the server itself. If you already had `srv` or `tsta` serving on 3004, we skip both the auto-start and the cleanup.

`fuser` comes from `psmisc` — preinstalled on Fedora. If for some reason it's missing, `sudo dnf install psmisc`.

---
I updated `cgn` again and it seemed to work; the Chromium browser opened and the terminal where `cgn` started returned to prompt when I closed that browser. I also updated the `cgn` section in [[tests/PLAYWRIGHT_SETUP#UI mode|PLAYWRIGHT_SETUP#UI mode]] and removed the unused `srv`. Should I also bump the `tests/helpers/package.json` version? What version would be appropriate?
```json
{
  "name": "@linebyline/test-helpers",
  "version": "1.0.0",
  "private": true,
  "main": "index.js"
}
```
After that, if there's nothing else, wrap up.

---
Yes — bump it. The export surface changed (`findLatestVersion` removed, `getAppUrl` added), which is a breaking API change for any consumer importing by name. Even though the package is `private` and all in-tree consumers were updated simultaneously, `2.0.0` correctly signals the break.

```json
{
  "name": "@linebyline/test-helpers",
  "version": "2.0.0",
  "private": true,
  "main": "index.js"
}
```
**`2.0.0`** — the right bump for a breaking export rename. After that, you're done.

**0-Roadmap item 1 recap** — what this session covered:
- Sanity-checked Qwen Coder's `playwright.yml` and `index.js` (3 critical, 2 high, 2 medium, 3 low findings)
- You applied the fixes (Node 24 env, SHA pins, preserved helper exports, settle-wait)
- Updated `cgn` with server lifecycle management
- Bump `@linebyline/test-helpers` to `2.0.0`

**Next roadmap item:** 2 — ESLint in Zed. That one's described as a 30-minute setup, and it's parallel with item 1 (which is now done).

---
I forgot to mention a few other things:
- In CONTRIBUTING.md I updated "Put each new version of LineByLine and the associated chat transcript into its own semantically numbered folder in [/archive/semantic](https://github.com/amokprime/linebyline/tree/main/archive/semantic)." → "Put the associated chat transcript for new version of LineByLine into its own semantically numbered folder in [archive/semantic](https://github.com/amokprime/linebyline/tree/main/archive/semantic).  Overwrite [docs/index.html](https://github.com/amokprime/linebyline/tree/main/docs/index.html) with the actual `.html` file."
- I think [[ai/z-ai-glm/skills/project-workflow-SKILL|project-workflow-SKILL]] still assumes the `.html` file will be named like `linebyline-X.X.X.html` when it would actually be more helpful to name it `index.html` every time so I can simply paste and overwrite the existing one in Dolphin
- [[archive/modular/plan/2-Zed-ESLint|2-Zed-ESLint]] was actually covered before step 1 (though nonfunctional for app code until modular refactor). The transcript for this session would go into the same folder as `1-Playwright-CI`.
- I want to retroactively delete the numerous existing `.html` files in `archive/` to declutter my repo. They'd still be in commit history if I really needed to check one out. But moving forward, I'd be checking out previous versions of `docs/index.html`.
- The `Archive.zip` persisting across sessions is a new bug I've noticed in the past few days. I suspect it must be resolved pre-upload by me (renaming the zip file from the default `Archive` name) and not something the agent can control.

---
Good updates — let me address each:

**1. CONTRIBUTING.md** — Clean. The new wording correctly splits the two concerns: transcripts stay versioned in `archive/semantic/`, the HTML lives at `docs/index.html`.

**2. project-workflow-SKILL.md naming** — Yes, this needs updating. The current pattern `archive/semantic/X.X.X/linebyline-X.X.X.html` should become `docs/index.html` throughout that file. The benefit of always naming it `index.html` is real — Dolphin paste-and-overwrite is one operation instead of delete-then-rename. And `getAppUrl()` already hardcodes `/docs/index.html`, so the skill and the code will be aligned.

**3. 2-Zed-ESLint sequencing** — Noted. So the roadmap reality is: item 2 was done first (even if nonfunctional for app code), item 1 is now done, and the transcript goes into the same folder as `1-Playwright-CI`. You might want to update the dependency graph in `0-Roadmap.md` to reflect that ESLint is already in place — maybe mark item 2 as ✅ with a note "nonfunctional for app code until modular refactor."

**4. Retroactive deletion of `archive/semantic/*.html` files** — This is the right call. They're in git history, so `git checkout HEAD~N -- docs/index.html` (or `git show REV:docs/index.html`) retrieves any old version. The one-liner to delete them all:

```sh
find archive/semantic -name 'linebyline-*.html' -exec git rm {} +
```

Then commit with something like `chore: remove versioned HTML files from archive/semantic — now at docs/index.html`. The `archive/semantic/` directories themselves (with chat transcripts) should stay — only the `.html` files get removed.

**5. Archive.zip cross-session persistence** — Agreed, that's a platform bug. Renaming before upload is the right workaround. Nothing the agent can do about it.

Nothing for me to deliver here — you've already made the changes. Just the `project-workflow-SKILL.md` update and the `git rm` cleanup remain on your end.

---
Trashed the archive html files. Update `project-workflow-SKILL.md` and retroactively update the session transcript with the last turn and this one.

---
Now I have the full skill file. Let me apply the edits:
Good, edits applied. Now let me copy it to download and also update the session transcript:
Now let me retroactively update the session transcript (`playwright-ci-sanity-check-v1.md`) with the last two turns:
Done. Two files in download:

- **`project-workflow-SKILL.md`** — 5 edits: version discovery targets `docs/index.html`, version is in `<title>` not filename, overwrite in place, companion naming derived from semver, download copy updated. Drop into `ai/z-ai-glm/skills/`.
- **`playwright-ci-sanity-check-v1.md`** — retroactively appended §8 covering Turns 7–9 (fish functions, package bump, archive cleanup, skill update).