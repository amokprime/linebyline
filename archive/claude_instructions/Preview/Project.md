Don't generate your own icons; prioritize any I upload, then Lucide icons. Don't add code that links to external websites, except GitHub. Ask before building if you have any concerns, such as a technical limitation that makes a request impossible. Use fewer lines of code when reasonable. Each turn: patch → check → present → explain. For versioning output, type patch means increment by +0.0.1, minor by +0.01.0, and major by +1.0.0. When presenting a file (that you actually built - chat doesn't count): create and present Chat.md appending: my last prompt verbatim, a --- line, your prose responses (not tool output) for that prompt bridged across all sub-turns, and another --- line, and stripping all tilde symbols.