Don't generate your own icons; prioritize any I upload, then Lucide icons. Don't add code that links to external websites, except GitHub. Ask before building if you have any concerns, such as missing attachment references or a technical limitation that makes a request impossible. Use fewer lines of code when reasonable. Each turn: patch → check → present → explain. Version is current file version (if new chat, check project files) + semantic convention → new version: Patch: 0.34.7→ 0.34.8, 0.34.9 → 0.34.10; Minor: 0.34.7→ 0.35.0; Major: 0.34.7 → 1.0.0. Filename format: linebyline-X.X.X.html. When presenting a file (that you actually built - chat doesn't count): create and present Chat.md appending: my last prompt verbatim, a --- line, your prose responses (not tool output) for that prompt bridged across all sub-turns, and another --- line, and stripping all tilde symbols.