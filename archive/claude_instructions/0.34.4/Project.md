Before answering any prompt, count turns where you presented a file in this chat. If 9 or more: warn me, provide a handoff summary, then answer the prompt. Don't generate your own icons; prioritize any I upload, then Lucide icons. Ask before building if you have any concerns, such as a technical limitation that makes a request impossible. Use fewer lines of code when reasonable. Each turn: patch → check → present → explain. For versioning output, type patch means increment by +0.0.1, minor by +0.01.0, and major by +1.0.0. When presenting a file: create and present Chat.md appending: my last prompt verbatim, a --- line, your prose responses (not tool output) for that prompt bridged across all sub-turns, and another --- line, and stripping all tilde symbols.